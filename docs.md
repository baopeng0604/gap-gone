# Gap Gone 产品与实现说明

## 产品定位

Gap Gone 是面向博客作者和内容创作者的本地录音、去静音和轻量音频剪辑工具。它保留语音备忘录的简单流程，同时提供可视化波形和人声降噪。

工具栏吸顶显示、宽度自适应：上排放去静音、降噪、转录、导出和帮助；下排放打开、录音、设置、播放及选择/切除/恢复/撤销/重做。详见「界面布局与交互」一节。

## 录音流程

1. 在录音设置中选择输入设备，点击“确定”关闭设置窗口。
2. 点击录音后先进入 2 秒准备倒计时（前 1.5 秒显示「准备」，最后 0.5 秒显示「开始」，与提示音大致对齐；提示音全长 2.68 秒，开录瞬间尾音会被掐断，后续计划换更短的提示音）。倒计时期间不采集音频，并可点击取消。
3. 倒计时结束后开始录音，观察实时 RMS、Peak 和峰值保持 dBFS 电平。最长可录 30 分钟，到达上限会自动停止并保留已录内容（界面有提示）。
4. 录音中可以暂停/继续、取消或停止。取消会丢弃当前录音并返回编辑页。
5. 可选开启耳机监听。默认关闭，避免扬声器反馈。
6. 点击停止录音后预览当前录音。
7. 点击取消会删除临时录音；点击确定并编辑会进入波形编辑页。
   录音相关按钮会直接显示对应按键提示：暂停/继续 `Space`、取消 `Esc`、停止 `S`；顶部录音按钮显示 `R`。

录音采样率跟随设备默认格式（Windows WASAPI 与 macOS CoreAudio 都只接受设备原生格式，常见为 48 kHz，部分 USB 麦为 44.1 kHz），统一在回调中下混为单声道 PCM WAV。macOS 和 Windows 使用 Rust/CPAL 原生采集，浏览器开发环境保留 Web Media API 回退。

录音电平中，-6 dBFS 以上显示黄色预警；检测到数字削波时，右侧 CLIP 标记会锁存为红色。点击红色标记只会清除提示，不会修改录音。
长条电平条按 dBFS 从 -60 到 0 每 6 dB 标出一格，越靠右代表电平越高、越接近 0 dBFS。

### 录音流稳定性：buffer underrun/overrun 修复记录（2026-08-27）

**现象**：Windows 录音过程中报错 "A buffer underrun or overrun occurred"，录音中断；报错后再次点击录音被「已经有录音正在进行」拒绝，已录内容无法取回。

**存在的原因**

1. 报错本体是 WASAPI 的 `AUDCLNT_E_BUFFER_ERROR`：音频回调必须在单个缓冲处理周期（约 10ms）内完成，否则采集端点缓冲区被覆盖。
2. 原实现在 cpal 实时回调里做了三件重活：加 `Mutex` 锁、逐样本写磁盘（WAV）、每个音频块跨进程 emit 一次 `recording-level` IPC 事件（约 100 次/秒）。任何一项超时都会触发该错误。
3. 错误回调只向前端透传消息，不回收 Rust 侧 `RecordingManager` 状态：流已死但 `active` 仍为 `Some`，导致重录被拒、临时 WAV 永不 finalize。
4. 与「旧 buffer 未清空」无关：每次新录音都会新建独立的采集流与 WASAPI 端点缓冲，旧缓冲随流销毁。

**如何修改**

* `src-tauri/src/lib.rs`：

  * 实时回调只做下混 + 经 mpsc 通道入队；新增 `gap-gone-wav-writer` 线程负责磁盘写入与电平统计。

  * 电平事件按 \~100ms 聚合后 emit，不再逐音频块发送。

  * 流报错后由独立线程 `gap-gone-recording-recovery` 回收：停掉失效流、finalize 已写入的部分录音、通过 `recording-error` 事件把 `{message, path}` 发给前端。错误回调本身只置标记，避免在音频线程内 drop 流导致自 join 死锁。

  * `start_recording` 遇到已标记 errored 的残留状态会自动清理，不再卡住重录。

  * `stop_recording` / `cancel_recording` 改为向写入线程发送 Finalize 指令并等待回执；通道 FIFO 保证停止前所有采样块已写盘。

* `src/useRecorder.ts`：

  * `recording-error` 载荷改为 `{message, path}`：读到部分录音时直接进入试听页并提示中断原因；读不到时维持原错误提示。

**潜在的风险**

1. mpsc 无界队列：写入线程被磁盘或杀毒软件卡住时，内存按约 192 KB/s 增长（48 kHz 单声道 f32）。必要时可改为有界通道 + 丢块计数，当前不做。
2. `stop_recording` 等待写入线程回执，磁盘彻底 hang 时命令不返回；与旧实现的同步 finalize 风险等价，非新增。
3. 电平表粒度从 \~10ms 变为 \~100ms 聚合；削波检测不会漏判（窗口内取峰值最大值），仅显示粒度变粗。
4. 录音中设备错误的 UI 行为变化（全平台）：从错误提示页改为进入试听页并保留中断前的部分录音，提示「录音中断，已保留中断前的部分录音」。
5. 平台差异：若 macOS CoreAudio 某些过载只丢包不触发 cpal 错误回调，自动回收不会触发，界面停留「录音中」需手动停止；原实现同样未覆盖此场景。

macOS 回归建议：正常录音→停止→试听、暂停/继续、录音中拔出设备、降噪链路。

**复发补充（2026-08-28）**：修复上线后仍有用户几分钟内触发同一报错。根因：当时的策略是「任何流错误都拆流」，而 WASAPI 的 underrun/overrun（cpal 报 `ErrorKind::Xrun`）是**瞬时毛刺**——系统调度抖动、杀毒扫描、DPC 延迟都可能触发，只丢极少量采样，流本身还活着。二次修复：错误回调按 `error.kind()` 分流——`Xrun` 只计数（随 `recording-level` 事件的 `glitches` 字段上报，录音界面显示"采集毛刺 ×N"），录音继续；只有设备拔出/流失效等真致命错误才走回收保留流程。

### 跨平台兼容与 IPC 传输修复记录（2026-08-27）

**背景**：审核发现三个跨平台风险，本次全部修复。

**1. 非 48 kHz 设备降噪静默降级**

* 现象：`denoise_audio` 只接受 48 kHz 单声道，而录音直接采用设备默认采样率。macOS 上大量 USB 麦克风默认 44.1 kHz，这些设备会无声地退回效果较弱的「兼容性降噪」，界面无提示。

* 修改：`src/utils/noiseReduction.ts` 新增 `renderBuffer`，用 OfflineAudioContext 在送 DeepFilterNet3 前重采样/下混到 48 kHz 单声道，处理完再还原回原始采样率与声道数。桌面端任何设备格式都走 DeepFilterNet3，结果消息仍会标明实际使用的引擎。

**2. 大文件 IPC 传输卡死风险**

* 现象：`read_recording` 与 `denoise_audio` 用 `Vec<u8>` 收发 WAV 数据，Tauri v2 自定义命令参数走 JSON 数字数组序列化；10 分钟录音约 56 MB，会展开成数千万个数字，内存与序列化开销不可接受。

* 修改：改为「临时文件 + 路径传参」。

  * `src-tauri/src/lib.rs`：删除 `read_recording`；新增 `prepare_denoise_files`（由 Rust 生成合法的 `gap-gone-*` temp 路径对）；`denoise_audio` 改为收 `inputPath`/`outputPath`，从磁盘读入、写出到磁盘；路径校验统一收敛到 `validate_temp_recording_path`。

  * 前端：读录音与降噪结果改用 `@tauri-apps/plugin-fs` 的 `readFile`/`writeFile`（二进制 raw 传输，不经 JSON 数组）；capabilities 增加 `fs:allow-temp-read` / `fs:allow-temp-write`。

  * 降噪结束后前端经 `delete_recording_file` 清理两个临时文件；取消/失败路径在 `finally` 里同样清理。

**3. CI 只有 Windows 构建**

* 新增 `.github/workflows/build-macos.yml`（macos-latest，Apple Silicon），与 Windows 流水线同结构（npm ci + rust-cache + tauri build），手动触发。改 df-tract/tract 等重依赖后两条流水线都要跑。

* 注意：macOS 产物为 ad-hoc 签名，自用没问题；对外分发会被 Gatekeeper 拦截，需要另配 Apple 开发者证书与公证流程。

**4. 三项细节优化（2026-08-27 晚）**

* **CSP 安全基线**：`tauri.conf.json` 原为 `csp: null`。现配置生产严格 CSP（`script-src 'self'`、禁内联脚本）+ 独立 `devCsp`（放行 Vite HMR 的 `ws://localhost:1420` 与 React 内联 preamble）。注意 Tauri 规则：只设 `csp` 不设 `devCsp` 时开发模式会套用生产 CSP，HMR 直接挂掉。

* **设备 ID 稳定化**：录音设备选择原先直接用设备名当 ID，Mac 上接两台同名 USB 麦会选错。现改用 cpal `Device::id()`（WASAPI endpoint ID / CoreAudio UID），名称只做显示与兜底匹配。

* **DeepFilterNet 模型缓存**：`denoise_audio` 原先每次调用都重建 DfTract（解析 tract 图 + 初始化运行时，数秒等待）。由于 DfTract 内含 `Rc` 不是 `Send`，无法放进 Tauri State，现改为常驻 `gap-gone-denoise` 工作线程缓存复用，命令只投递任务。复用前通过 `init()` + `DFState::reset()` + `init_norm_states()` 重置流式状态，防止上一段音频的归一化状态串扰。首次加载时 Rust 发 `denoise-progress = -1`，前端显示「正在加载降噪模型…」而不是卡在 0%。

### 临时目录归类 / 设置持久化 / MP3 导出（2026-08-30）

**1. 临时文件统一归档到 temp/gap-gone/（0.1.4）**

* 录音、降噪、转录的全部临时文件从系统 temp 根目录移入 `temp/gap-gone/` 子目录（Rust 侧 `gap_gone_temp_dir()` 统一生成，写前 `create_dir_all`）。`validate_temp_recording_path` 安全校验同步收紧为 parent 必须是该子目录。

* 设置页新增「临时文件目录」状态行：显示占用空间与文件数量（async 命令 `temp_storage_status`，磁盘遍历放 `spawn_blocking`）+ 一键清理按钮（`clear_temp_files`）。正在录音的文件因写入线程持有句柄删除会失败，自动跳过，不会误删进行中的录音。

* 设置页「确定」按钮移到第一行（录音设备行）右侧。

* **临时文件生命周期（排查结论，2026-08-31）**：录音边录边写 `temp/gap-gone/gap-gone-*.wav`；点停止进入试听的瞬间，前端 `readFile` 把整个文件读进内存变 Blob 后**立刻 `delete_recording_file` 删除磁盘文件**（`stopRecording` 的 `.finally()`）——所以「录了话 → 不点确定 → 直接取消」**不产生残留**，取消只丢弃内存副本。录音中取消走 `cancel_recording`（Rust 侧 `remove_file`）同样干净。唯一残留场景：录音进行中应用崩溃/被强杀（`finally` 没机会执行），此时靠设置页一键清理兜底。

**2. temp 子目录的 fs 权限坑（0.1.5，踩坑记录）**

临时文件移入子目录后录音报「无法完成录音文件」。根因：capabilities 的 `fs:allow-temp-read` / `fs:allow-temp-write` **只授权 temp 顶层文件，不含子目录**（Tauri v2 ACL 定义）。修复：追加 `fs:allow-temp-read-recursive` / `fs:allow-temp-write-recursive`。教训：凡涉及 fs scope 的路径层级变化，capability 要同步评估递归权限。capability 是构建期注入的，改动后需完全重启 `pnpm tauri dev`。

**3. 转录模型默认目录改为 \~/models/sense-voice（0.1.6）**

`transcribe.rs` 的 `model_dir()` 默认值从 `app_data_dir()/models/sense-voice` 改为 `home_dir()/models/sense-voice`（用户自定义目录优先级不变）。注意：旧位置已有模型的用户需手动迁移，否则首次转录会重新下载 230MB。

**4. 倒计时界面文案与音频解耦（0.1.6，0.1.12\~0.1.15 持续调优）**

倒计时大字从数字「3、2、1」改为「准备」/「开始」。经多轮手感调优后的最终节奏（0.1.15）：**总时长 2 秒，0.25s 步进计时，「准备」1.5 秒（remaining 8→3）、「开始」0.5 秒（remaining 2→1）**。实现要点：`startRecordingWithCountdown` 里 `remaining` 以 0.25s 为单位从 8 递减到 0，界面阈值 `recordingCountdown > 2` 切换文案——要调节奏只改初值和阈值两处。已知取舍：countdown.mp3 全长 2.68s，开录瞬间尾音被掐断（pause 兜底防止外放被录进开头），换 2 秒以内的短提示音即可完全对齐。

**5. 设置持久化（0.1.7）**

新建 `src/utils/settings.ts` 统一管理用户偏好（localStorage，Tauri WebView 按 app identifier 隔离落盘）。新增持久化的设置：录音设备（cpal 稳定 id，设备被拔时回退默认设备）、静音检测预设、降噪预设、转录面板显隐、导出格式、MP3 码率。已有的 `gap-gone-auto-transcribe` / `gap-gone-model-dir` 两个 key 沿用不迁移。选型说明：备选 `tauri-plugin-store`（JSON 文件）需要 Rust+JS 双端依赖与异步初始化竞态处理，对当前体量属过度设计；如未来要做设置导出/同步再迁移。

**6. MP3 导出（0.1.7）**

* 编码器 `@breezystack/lamejs`（lamejs 维护分支，纯 JS 无 wasm，CSP 零改动）。`src/utils/mp3Export.ts` 按 LAME 1152 采样块分批编码，每 100 块让出主线程（`setTimeout 0`），长录音编码期间 UI 不冻结（导出按钮复用 `isProcessing` 禁用）。

* 设置页新增「导出格式」：WAV（默认）/ MP3 单选 + MP3 码率单选（96 / 128 推荐 / 192 kbps CBR mono）。导出按钮文案、保存对话框 filter、默认文件名扩展名均跟随格式。

* 码率参考：语音场景 128 kbps mono 已达人耳听感透明（约 0.9 MB/分钟，为 WAV 单声道 5.76 MB/分钟的 1/6）；纯语音体积优先选 96；音乐为主选 192。

* 注：lamejs 若因环境限制无法走 pnpm 安装（如 TRAE 沙箱拦 store），可从 registry 下载 tarball 手动解压到 node\_modules 并登记 package.json，但 `pnpm-lock.yaml` 需事后在正常终端 `pnpm install` 补齐。

**7. bufferToWav 两个历史 bug 修复（0.1.7）**

* 头部写入变量 `pos` 被数据循环复用，导致每次 WAV 导出丢失开头 44 帧（约 0.9ms）音频、末尾多 44 帧零。改用独立帧索引。

* 16-bit 缩放表达式 `(0.5 + sample < 0 ? ...)` 因运算符优先级实际求值为 `(0.5 + sample) < 0`，仅低于 -0.5 的采样走 32768 分支。修正为标准 `sample < 0 ? sample * 32768 : sample * 32767`。

### 转录文字稿增强：智能命名 / 复制 / 分段 / 标点（2026-08-31）

**1. 导出文件名关键词提取（0.1.8）**

导出文件名从固定 `YYYYMMDD-HHmmss-edited-audio.mp3` 改为 `关键词-日期-时间.mp3`。关键词由 `src/utils/keywordExtract.ts` 从转录逐字稿离线提取（≤7 字）：**WebView 原生 `Intl.Segmenter` 分词（零依赖）+ 中英停用词表过滤 + 词频统计**。关键细节：ICU 分词会把「人工智能」切成「人工/智能」，加了**相邻二元词合并**（成对出现的相邻中文实义词合并还原完整术语）。无转录/纯虚词/老系统无该 API 时静默回退日期命名。TS lib（ES2020）无 Intl.Segmenter 类型，文件内做了最小类型声明。导出为纯前端改动，Rust 零变更。

**2. 转录面板复制与 TXT 自动分段（0.1.9）**

- 转录栏新增「复制 TXT」按钮（`copyTranscriptText`，`navigator.clipboard` + `execCommand` 兜底，成功后按钮变「已复制」2 秒）。
- `buildTxt` 重写为自动分句分段：① 相邻句语音停顿 ≥1.5s 视为话题边界换段；② 段落积累 120 字后在句末标点处收段；③ 200 字硬上限强制分段。段内碎片自然拼接（中文直连、英文词间补空格），段落间空行。导出与复制共用同一逻辑。

**3. 提示条自动消失（0.1.9）**

`App.tsx` 新增 `notify(message, kind)` 统一提示机制（全部 20 处收口）：info 类（成功/结果通知，如「转录完成」「导出成功」）3 秒自动消失；error 类（失败）常驻手动关闭。新提示替换旧提示并重置计时。

**4. 转录标点符号恢复（0.1.10，Rust 侧）**

SenseVoice 输出标点稀疏（长音频切块识别的块边界必丢标点）。方案：**sherpa-onnx crate 自带的 `OfflinePunctuation` 模块**（项目现有依赖，零新增）+ 官方 CT-Transformer 中英混合模型（int8 量化 75MB）。实现：

- 模型运行时下载到 `~/models/punctuation-ct-zh-en/`（HF 单文件镜像 ranger810 repo，主站+hf-mirror 双源、`.partial` 过渡、进度走 `transcribe-progress` 事件新 stage `punctuation`），常驻 `gap-gone-transcribe` 工作线程复用。
- 应用点：识别完成后逐 segment 补标点（`TranscriptResult` 新增 `punctuated` 字段）。**词带不加标点**——锚定波形时间戳。
- 降级：下载/加载失败 → 跳过标点正常返回转录 + 前端常驻提示；下载失败后会话内不重试（避免每次转录等网络超时），手动放置模型文件后仍可恢复。
- TXT/SRT/句子面板/关键词提取全部自动受益。

**5. SRT 复制与分句优化（0.1.11）**

- 转录栏新增「复制 SRT」按钮（`copySrtText`，与复制 TXT 共用 `copyText` 公共函数，按钮反馈区分 TXT/SRT）。
- `buildSrt` 重写为两步分句分段：① **拆长条**——segment 内部含多个句末标点（标点恢复后常见）时拆成单句字幕，时间戳按字符占比线性插值（误差远小于字幕可读阈值）；② **合并碎句**——相邻句停顿 ≤0.6s 且合并后 ≤42 字符（约两行 21 字）/ ≤7s 时合并为一条。实测：10 秒三句长条正确拆为三条、0.2s 停顿碎句正确合并、3s 话题边界不合并。

**6. 降噪输出响度说明（-4.2 dB 现象排查，无代码改动）**

用户反馈降噪后峰值约 -4.2 dB，疑有限幅器。全链路排查结论：**代码中无固定增益/限制器**（录音直通、电平表纯测量、DF3 的 STFT 前端 vorbis 窗 50% overlap 完美重构、写出只 clamp ±1）。该响度下降是 DeepFilterNet3 模型行为：训练目标为 SI-SDR（信噪比）而非响度保持，语音段 mask 平均略小于 1，输出整体比输入低几个 dB 属正常。影响：无音质损害（远离削波）；导出响度偏小；整体电平低时静音检测 dBFS 阈值相对变宽松。另一嫌疑是系统麦克风级别（Windows 默认常为 85%\~90%）——录音中电平表若也卡 -4.2 则与App无关。如需修正，业界常规是降噪后峰值归一化到 -1 dBFS（RNNoise 管线惯例），后续可加。

## 界面布局与交互（2026-08-28 重构）

**工具栏**：`fixed` 悬浮改为 `sticky` 吸顶 + 文档流排布，宽度 `fit-content` 自适应（`min-width: 1100px`）。波形的 `padding-top` 和所有浮层卡片（录音倒计时/设置/试听、提示条）的写死 `top` 全部删除，改为跟在工具栏后自然流式排布——工具栏高度变化时下方内容自动跟随，不再反复错位。注意一个 CSS 坑：`.container` 必须用 `overflow-x: clip` 而不是 `hidden`，后者会创建滚动容器导致内部 sticky 全部失效。窄屏渐进降级：≤1120px 隐藏快捷键徽标，≤960px 隐藏分组标签。按钮更名：「恢复本次检测」→「恢复检测」（与编辑工具「恢复」区分）、「录音设置」→「设置」。

**转录面板**：`sticky` 吸底常驻，列表高度约 4 行（132px）内部滚动。波形区设最小高度，保证至少完整露出 3 行（含词带）。

**播放光标跟随**：播放头所在行滚出可视区时 `scrollIntoView` 到视口中间（平滑滚动）；行仍可见时不动，不打断用户浏览。按 10 秒行粒度检测。

**Space 误触修复（2026-08-28）**：工具栏按钮点击后焦点残留在按钮上，浏览器默认行为会用空格激活聚焦按钮（曾导致按 Space 误触发「选择」而非播放）。修复：键盘焦点守卫白名单加入 Space，keydown 阶段 `preventDefault` 阻断按钮激活，Space 永远只触发播放/暂停。

**设置页**：录音设备选择之外新增——转录模型目录（文本框显示当前生效路径，可自定义并保存，「打开目录」按钮在文件管理器中直达；自定义路径存 localStorage，启动时经 `set_transcribe_model_dir` 同步到 Rust 侧 `RecordingManager.transcribe_model_dir`）、「录音确定编辑后自动转录」开关（默认开启；实现上直接把解码后的 AudioBuffer 传给转录函数，避免闭包读到未更新的状态）、临时文件目录占用统计与一键清理（见 2026-08-30 记录）、导出格式与 MP3 码率。「确定」按钮位于第一行右侧。注意：切换到没有模型文件的新目录时，首次转录会向新目录重新下载 230MB，可手动把 `model.int8.onnx` + `tokens.txt` 复制过去。

设置页后来陆续加入（2026-09-22 汇总）：**静音阈值**（三档预设 + 手填 dBFS，改完自动清掉旧候选）、**响度目标**（三档预设 + 手填，默认 -19 LUFS，Apple Podcasts 单声道口径）。Windows 上首次启动（或保存的设备已拔掉）时录音设备默认选中列表第二个——内置麦通常排第一、用户想用的外接麦在第二位；其它平台仍取第一个，用户显式选过的设备不会被覆盖。转录模型文件齐备时「下载模型」按钮置灰不可点，避免重复下载；只有"转录齐、标点缺"时仍可点。模型目录那条的提交按钮由「保存」改名「应用路径」——它只把输入框里的路径写入配置并重查状态，不点就不生效，叫"保存"容易被误认成保存项目。

**设置持久化（2026-08-30）**：录音设备、静音/降噪预设、静音阈值、响度目标、转录面板显隐、导出格式与码率、自动转录、模型目录均经 `src/utils/settings.ts` 存 localStorage，跨启动保留；改完即时写入，不需要点「确定」（「确定」只关闭设置面板）。唯一例外是模型目录输入框，必须点「应用路径」才落盘；静音阈值的数字框是失焦时写入，输完直接按 Esc 关面板可能来不及触发失焦，那次输入不会保存。播放速度与循环不进这个体系——它们是会话内参数。

**录制时长上限**：最长 30 分钟，到达自动停止并保留已录内容（`useRecorder` 监听 duration，原生/Web 双路径共用）。`mac` 合入 `origin/sensevoice` 时由 15 分钟改为 30 分钟。

## Mac 合入待完成（2026-08-31）

`mac` 已合并 `origin/sensevoice`（SenseVoice 转录、新录音管线、新 UI）。当前目标是开发者本人在 Apple Silicon 上自用，ad-hoc 签名即可；公证、Intel Mac、沙盒网络权限不在本轮。

**本轮验收（只这三项）**

1. Apple Silicon 上能编过、能跑。
2. 内置麦克风：倒计时 → 录音 → 停止 → 试听 → 确定编辑。
3. 能完成一次转录（模型默认 `~/models/sense-voice/`，首次会联网下载）。

**待完成（不阻塞本轮合入）**

1. **外置/USB 麦全流程**：倒计时 → 录 → 暂停/继续 → 停止 → 试听 → 确定编辑。
2. **非 48 kHz 降噪重采样**：尤其 44.1 kHz USB 麦，确认仍走 DeepFilterNet3，而不是静默落到弱化兼容路径。
3. **录音中拔掉外置麦**：应进入试听、保留已录内容，并给出明确错误提示。
4. **采集看门狗（设计已定，尚未实现）**
   - 全平台，不单独 `cfg` macOS。
   - 仅在未暂停、正在录音时计数；倒计时不采集，不计入。
   - 判定的是「采集回调连续不再送来任何采样块」，不是房间静音（静音仍会推零）。
   - 连续 **5 秒** 无新采样 → 复用现有中断回收：停流、finalize 已写入 WAV、前端进入试听并保留已录内容。
   - 常驻错误文案必须含：「已连续 5 秒未收到音频数据」。建议整句：「采集中断：已连续 5 秒未收到音频数据，已保留中断前的录音。请点确定编辑，或取消后重录。」
   - 在看门狗落地前，macOS CoreAudio 某些过载可能只丢包、不触发 cpal 错误回调，界面会停在「录音中」，只能手动停止。
5. **更广设备与边界**：蓝牙麦、录满 30 分钟自动停止、两台同名 USB 麦的设备 UID 选择、断网时首次转录（标点下载失败应降级，转录本身不应整段崩溃）。

**已拍板、不当成新债**

- 模型默认目录保持 `~/models/sense-voice/` 与 `~/models/punctuation-ct-zh-en/`，设置里可改；不为 Mac 改到 Application Support。
- 对外 Gatekeeper / 公证、Intel Mac 以后再说。

## 编辑语义

波形每行表示 10 秒，默认窗口为 1280×820，单行波形宽度为 1000px；末行不足 10 秒时按实际秒数收窄行宽（`1000 × 实际秒数 / 每行秒数`），不拉满整行——拉满等于把这行的横轴从 3 秒摊到整行宽度，波形被横向拉伸，比例和其它行对不上。

* 选择工具：左键拖拽建立降噪选区。

* 切除工具：左键拖拽标记需要跳过的区域。

* 恢复工具：左键拖拽恢复已标记区域。

* 右键拖拽：兼容快捷方式，标记跳过区域。

* 中键拖拽：兼容快捷方式，恢复跳过区域。

* 去静音预设：提供紧凑、自然、宽松三档，默认自然，控制静音区间两端保留的声音。三档的完整数值（秒）：

  | 档位 | 静音最小时长 | 起点侧留白（上一句收尾后） | 终点侧留白（下一句起头前） |
  | --- | --- | --- | --- |
  | 紧凑 | 0.2 | 0.06 | 0.15 |
  | 自然（默认） | 0.3 | 0.12 | 0.25 |
  | 宽松 | 0.4 | 0.18 | 0.35 |

  **两侧留白不对称**，终点侧比起点侧大。原因是检测出的静音末尾容易越过下一句的起音：中值滤波会把孤立的起音块按邻居的安静值压回去，滞回又要求高出阈值 6 dB 才退出，两者叠加让区间多咬进几十毫秒语音，留白不足时下一句就听着"突然冒出来"。上一句收尾那边没有这个问题，保持较小留白即可。贴住音频开头/结尾的静音不设保留量——那里前后没有声音要保护，留一小截只会变成切不掉的尾巴。

* 静音阈值：设置里可调，默认 -36.5 dBFS（等价于最初硬编码的线性 0.015，不制造行为回归），范围 -60 ~ -20，另有两档预设「激进 -30」「保守 -45」。低于阈值才算安静，所以数值越小越严格。改动后立即清掉旧候选。

* 检测静音：逐块（4096 采样，44.1 kHz 下约 93 ms）算 RMS 并转 dBFS，先过窗口 3 的中值滤波抹掉单块尖峰，再用 **6 dB 双阈值滞回**判定——进入静音看阈值、退出要高出阈值 6 dB；最后把空隙 ≤ 0.15 s 的相邻静音区间合并，再套最小时长与两端保留量。候选结果先以预览层显示，不会立即修改编辑时间轴。

滞回与合并是为一个具体问题加的：底噪在阈值附近上下波动时，原先"块内一越线就结算、累计不足最小时长就整段丢弃"的判法会把一段 1 秒的安静切成若干 0.2 秒的碎块，每块都不到 0.3 s，于是一整段静音全部消失、候选数为 0。中值滤波杀尖峰、滞回防状态翻转、隙缝合并把碎块重新接起来，三者叠加后默认阈值下就能检出来；只有底噪整体都在阈值之上、从不跌破时，才需要上调阈值。

* 应用检测：确认候选区间后将其加入跳过区间；清除候选则放弃本次预览。

* 恢复本次检测：只移除最近应用的自动检测结果，不影响手动切除。

* 撤销/重做：编辑区间使用非破坏性历史记录。

“去静音/跳过”会同时影响播放和导出，输出时长会缩短；它不等同于保留时间轴的静音效果。原始 AudioAsset 始终保留。自动检测区间和手动切除区间分开记录，恢复自动检测时不会误删手动编辑。

### 快捷键

* `Space`：播放 / 暂停。播放中点击波形会跳转并继续播放，暂停时点击只移动播放头。

* `D`：检测静音并生成候选预览。

* `X`：切换到切除工具，再按一次返回默认（点按定位）模式。

* `C`：切换到恢复工具，再按一次返回默认（点按定位）模式。

* `N`：一键降噪；`B`：恢复原始（撤回已确认的降噪版本）；`T`：转录文字。

* `P`：从头播放（成片开头，已切除的区间跳过）。

* `L`：循环播放开关，点亮即从成片开头开始播放、再按熄灭并停止；`⇧L`：一键响度标准化，把成片 Integrated 响度归一至设置里的目标（默认 -19 LUFS）。

* `[` / `]`：播放速度减 / 加一档。

* `R`：空闲时开始录音。

* `Shift+R`：恢复最近一次已经应用的自动检测结果。

* `S`：录音中停止并进入录音结果预览。

* `⌘/Ctrl+Z`：撤销；`⌘/Ctrl+Shift+Z`：重做。

* `⌘/Ctrl+S`：导出音频（默认 WAV，可在设置中切换 MP3）。

* `O`：打开音频文件。

* `Enter`：录音结束后确定并进入编辑。

* `Esc`：取消录音倒计时、录音中取消并返回编辑页，或取消录音结果；帮助打开时关闭帮助。

* `H` 或 `?`：打开帮助。

## 播放引擎与左侧播放栏（2026-09-22）

**左侧播放栏**：工具栏已排满，播放速度、循环、从头播放单独成栏——`position: fixed; left: 1rem; width: 72px`，与右侧电平栏镜像，容器 `pointer-events: none` 只让控件可点，窄屏（≤1079px）跟右侧栏一起隐藏（窗口 `minWidth` 是 1100，正常不触发）。自上而下：从头播放（`⇤`，快捷键 P）→ 循环开关（`↻`，快捷键 L）→ 竖向速度滑条 → 倍速读数（点击复位 1.0×）。两个图标按钮默认白底黑字，循环点亮态用工具栏那个绿；速度滑条按档位索引吸附（不用小数步进，避开浮点误差），慢速冷蓝、1.0 中性灰、快速暖琥珀，用 `accent-color` 同时染滑条与读数，滑条旁每档一个刻度点。

**为什么换掉播放引擎**：原来的分段调度用 `AudioBufferSourceNode` 逐段 `start(0, offset, duration)` 拼接，位置靠 `sourceOffset + context.currentTime - startedAt` 推算。这套东西精度是样本级的，但 `AudioBufferSourceNode` 只有一个 `playbackRate`，语义就是磁带变速——变快必升调，做不出变速不变调。现在换成 `<audio>` 媒体元素：`preservesPitch`（并同时设 `webkitPreservesPitch`）由浏览器做时间伸缩，0.6～2.0 全档位即时生效、零 JS 计算代价；而且 `audio.currentTime` 本身就是源时间轴上的位置，波形游标与时间显示不需要任何换算——比原来的时钟差推算还简单。

**切除区间怎么跳过**：媒体元素没法"按区间拼接"，改成**保留段索引 + 段尾跳转**——`getKeptRegions` 切出的保留段序列由我们持有索引，rAF 里只把当前播放位置与「当前保留段」的端点比较，越过段尾才 seek 到下一段开头并推进索引。精度从样本级降到约一帧（16 ms），实际影响极小：最短的切除区间也有 0.2 s（静音检测）或上百毫秒（手拖）。几处容易写错的细节：索引必须先推进再发 seek（同一跳转不会重复下发）；段尾比较留 5 ms 容差；最后一段若一直延伸到音频末尾就交给 `ended` 处理，提前 stop 会把循环回环的机会掐掉；**播放中的用户跳转要重建播放会话**（`startPlayback`），否则段索引与新位置不一致，状态机会把播放头拉回它自己认定的当前段。

这里踩过一个真实的坑（0.1.38 修复）：最初写的是"每帧查一次当前位置是否落在切除区间里，是就跳到区间末尾"，结果**有静音片段的音频按 P 完全不出声、或者播到静音片段边界就卡住**。原因是规范允许脚本运行期间读到滞后的播放位置（MDN：the reported playback position must remain stable while scripts are running），于是同一个跳转目标被逐帧反复下发，媒体元素不停重启 seek，音画再也接不上。结论是跳转决策不能每帧从 `currentTime` 重新推导，必须由显式的段状态机驱动。

**播放源与内存代价**：媒体元素只能播 URL，而降噪、响度归一之后的缓冲只在内存里、没有磁盘文件。做法是统一把当前 `audioBuffer` 用 `bufferToWav` 编成 16-bit WAV blob，只在缓冲身份变化时重编码（打开/录完/降噪/响度/撤销）；切除区间不触发重编码，因为它只改 `deletedRegions`。编码是同步的，长录音会占住主线程一会儿，所以延到下一帧执行，先让"加载完成/处理完成"的画面画出来。代价是内存里同时存在 AudioBuffer 与 16-bit WAV 两份（30 分钟单声道 48 kHz 约 690 MB + 345 MB）；超长录音若撑不住，改「写临时 WAV + Tauri asset protocol」即可，改动面只在一处。

**循环与从头播放**：循环范围是成片（跳过 EditTimeline 后的内容），回环点是成片起点而不是文件 0——文件开头可能已经被切除。循环开关兼作播放控制：点亮即从成片开头开始播，熄灭即停止播放（点亮不自动播的旧口径已废弃）。从头播放走 `startPlayback(0)`，`0` 会被 `nextPlayableTime` 吸附到第一个保留区间起点，所以"从头"是成片开头。速度与循环都是会话内参数，不落 localStorage。

**快捷键调整**：`L` 从「一键响度标准化」改成循环播放开关，响度标准化挪到 `⇧L`（keydown 用 `shiftKey` 互斥，工具栏角标、`aria-keyshortcuts`、HelpModal 三处同步）。理由是循环属于边听边开的高频操作、响度标准化是收尾用一次的操作，而 L 又是 loop 的通用肌肉记忆；`⇧R` 已有先例。

**顺带修的坑**：`handleKeyDown` 的"文本输入焦点守卫"原先把 `input[type=range]` 也当文本框，拖完速度滑条后焦点留在滑条上会导致所有快捷键失效；已在选择器里排除 range（方向键换档仍走原生行为）。

**待实机验证（Windows 上验证不到的部分）**：

1. macOS WKWebView 的 `webkitPreservesPitch` 与 `-webkit-appearance: slider-vertical` 都是 Safari 分支，Windows 跑不到，需要在 Apple Silicon 上试。
2. 竖向滑条刻度与滑块的对齐：刻度容器按"滑块直径 16px"上下各内缩 8px 估算，WKWebView 的滑块尺寸和轨道内边距若不同，微调 `.playback-ticks` 的 `margin` 即可。
3. 0.6 与 2.0 是原生时间伸缩的边界区，听感会比中间档毛糙，这是算法边界而不是实现问题。
4. 窗口宽度在 1100～1256px 之间时左右两条侧栏都会压在内容列上（右侧栏本来就这样），默认 1280 窗口下两侧各有约 12px 空隙。

## 设置面板与界面细节（2026-09-22）

* **设置面板的「取消」按钮**：设置项是改完即时写入 localStorage 的，本来没有"未提交"这回事，所以「取消」不是"关闭而不保存"，而是**回退**——面板打开时给面板内的即时生效项拍一张快照（`setupSnapshotRef`），点「取消」按快照写回：录音设备、自动转录、显示转录面板、静音阈值、响度目标、导出格式、MP3 码率、转录模型目录。工具栏上的静音预设与降噪预设不在面板内，不参与回退（面板内也没有它们）。两个必须留意的细节：① 快照只在 `showRecordingSetup` 变为 true 时拍一次，面板内的后续改动不能覆盖它（所以那个 effect 的依赖故意只写开关）；② 模型目录存在后端、要异步取回，快照里该项为 `undefined` 就跳过回退，否则"打开面板后立刻点取消"会把自定义目录冲成默认。

* **波形末行按秒数收窄**：`WaveformScore` 的行宽从写死的 1000px 改成按 `1000 × (endTime - startTime) / secondsPerRow` 计算。收窄后 px/秒 与其它行一致，点击、拖拽、播放头、词带的时间换算都走同一个 `width` 入参，不受影响。

* **右侧栏「回到顶部」按钮改成白底黑字**，与左侧播放栏按钮同一套配色（底 `#f2f2f2`、字 `#101510`、hover `#dcdcdc`）。右侧栏其它元素都是暗色，这个按钮要更跳一些才容易被看到。

## 降噪

桌面应用内置 DeepFilterNet3 模型（**低延迟变体**：标准变体的图在当前锁定的 tract 上编译不过，这是能否运行的问题，详见 `src-tauri/Cargo.toml` 的注释与 AGENTS.md 硬约束 18）。模型使用 Rust 原生处理链，在 48 kHz 单声道音频上执行离线处理；录音为其他采样率或立体声时，前端会在处理前后自动重采样转换，任何设备都能使用模型降噪。模型约 8.5 MB，随应用分发，不需要联网下载。

降噪提供轻、中、强三档。没有选区时处理整段音频，有选区时只替换选区。降噪结果先进入试听预览，确认后才成为当前版本，取消试听会恢复处理前版本。

**DeepFilterNet 不可用时会明确告知（0.1.57）**：处理失败不再静默换算法，界面会写出失败原因，并说明已退回「基础降噪」、效果会明显更差。基础降噪只做一件事 —— 按整段电平的低分位数估出底噪，把低于「底噪 + 余量」的部分按档位衰减，增益带平滑（它没有 DeepFilterNet 的深度滤波与 ERB 掩蔽，效果差一个量级）。

中间文件走 **32-bit float WAV**，不是导出用的 16-bit：口播录音的有效电平常在 −30 dBFS 量级，16-bit 会把底噪那一段量化掉，而模型决定「压多少」的依据正是对底噪的估计 —— 精度先丢，抑制质量跟着掉（0.1.52 修）。前端读回结果也不走 `decodeAudioData`，直接按 WAV chunk 自解析：格式是自己写的，少一次编解码往返，也不依赖 WebView 对 IEEE float WAV 的支持。选区降噪在边界做 10 ms 交叉淡化 —— 降噪结果与原始在接缝处电平/相位不连续，硬拼接会留下咔嗒声。

**处理强度按帧由三个 local-SNR 阈值决定**（crate 的 `apply_stages`）：帧的 lsnr 高于 `max_db_erb_thresh` 就**整帧原封不动**（连增益都不算），落在 `max_db_df_thresh` 与 `max_db_erb_thresh` 之间则只做 ERB 掩蔽、**跳过 DeepFilter 第二级**，低于 `min_db_thresh` 直接清零。此前用的是库默认（−10 / 30 / **20**），把口播里「有语音、底下压着一层底噪」的帧（local SNR 约 20 ~ 35 dB）划进了「跳过 DF」甚至「整帧不动」—— 等于降噪在最需要它的地方被提前关掉。实测表现就是用户反馈的「一说话噪声更明显」。现已对齐官方 `enhance_wav` / C API 的口径（−15 / 35 / 35）。另外 `DfTract::process` 的输出相对输入滞后 `fft_size − hop_size + lookahead × hop_size`（当前模型 = 960 − 480 + 0 = **480 采样 = 10 ms**，与官方 `enhance_wav` 的公式一致）。官方 CLI 用 `--compensate-delay` 丢掉这一段；我们同样补偿（输入尾部补零、输出整体前移），实测补后与原始时间轴逐采样对齐（原始波形互相关的峰值落在 lag 0）。不补的话整段降噪会整体滞后，与视频/字幕对不齐，**选区降噪更会直接错位**（交叉淡化只盖得住电平跳变）。**公式里不要加 `df_order`** —— 0.1.59~0.1.60 曾写成 2880 并声称「官方漏了一项」，那是错的，多出来的 2400 来自下面那套重置。另外**每个降噪任务都会重新加载模型**（不缓存复用），并**绝不调用 `init()` / `DFState::reset()` / `init_norm_states()`** —— 这三行会清掉模型内部的滚动缓冲与归一化状态（详见「降噪接线修复（0.1.61）」小节）。

DeepFilterNet3 主要面向人声背景噪声。它不是无损处理器，强度过高时可能产生金属音、吞字或过度抑制环境声。**当前内置的是低延迟变体（约 35 MB；标准变体只有 7.6 MB）—— 这不是性能取舍，而是标准变体的计算图在当前锁定的推理引擎版本上连加载都会失败**（详见 `src-tauri/Cargo.toml` 的注释与 AGENTS.md 硬约束 18）。多出来的体积换来的是降噪真正能跑起来。

三档强度的实际口径要说清：DeepFilterNet 的 `atten_lim` 不是「每频段最多压 N dB」，而是**把原始含噪频谱按 `10^(−N/20)` 的比例全频段掺回输出** —— 轻档 12 dB 掺回 25%、中档 24 dB 掺回 6.3%、强档 36 dB 掺回 1.6%（原本给的是 100 dB，等于「不限、纯降噪」，0.1.56 起封顶在 36 dB，理由是「实测会把人声压出破音」。**0.1.60 曾用一批实测推翻这个理由、断言「24 dB 以上整段阶梯空转」，0.1.61 又把那次断言推翻了** —— 那批数据是在还带着降噪接线 bug 的条件下跑的（见「降噪接线修复（0.1.61）」）。去掉 bug 后重测：停顿段抑制量 轻 10.4 / 中 15.9 / 强 19.4 / 极限48 20.4 / 不掺回100 20.2 dB，**阶梯有效到约 48 dB 才饱和**。所以「轻档几乎等于没降」是它的字面行为、不是错觉，但「中档以上没区别」是假象）。DFN3 的 post-filter **保持关闭**（`set_pf_beta(0.0)`）。它会在「掩蔽不确定」的频点上再压一层（官方 CLI 默认 0.02），但代价是语音失真：0.1.54 试开 0.02，低信噪比素材上残余噪声没消掉、人声先发毛/破音，0.1.55 回退。**注意链上顺序：post-filter 在「掺回原始噪声」之前跑**，所以它本来也压不掉档位掺回去的那部分，那部分只能靠档位来减（强档不掺回）。

**这套参数有天花板，别指望旋钮救回低信噪比素材。**（0.1.61 重测，**结论比 0.1.60 乐观**：停顿段抑制量 轻 10.4 / 中 15.9 / 强 19.4 / 极限 48 20.4 / 不掺回 100 20.2 dB —— 档位一直有效到约 48 dB 才饱和，语音段只降 1.5 ~ 1.6 dB。0.1.60 那次「中档以上只差 0.3 dB、抑制卡在 9.5 dB」的数据是带着降噪接线 bug 跑的，见下一小节。）取舍只有一条：`atten_lim` 小就留残余噪声（中档掺回 6.3%），档位拉满（强档不掺）则是模型全力抑制，低信噪比素材上会吐出谱洞与帧间抖动 —— 听感就是「人声破音 + 噪声跟着语音起伏」。当「中档留残余」与「强档出伪影」同时成立，说明该素材已经到头了，再调档位、再开 PF、再改阈值都只是在这个取舍上滑动；真正的解法在录音端：把输入峰值定到 −12 ~ −6 dBFS，让底噪离语音远一点（链路尾部的扩展器与归一都救不回信噪比）。定位方法是看「压缩」播报里的**成片有效电平**与**噪声底**，两者之差就是信噪比：≥ 30 dB 算够干净，15 ~ 25 dB 就是底噪贴太近。

### 降噪接线修复（0.1.61）

**问题。** 用户反馈「录完音直接用降噪，人声部分明显异常、基本不可用」。

**排查与根因。** 拿一条真实录音，用作者自己的官方参考实现（crate 自带的 `deep-filter` bin，`libDF/src/bin/enhance_wav.rs`，同一个模型、同一档位）跑同一段素材做对照，逐块比 5–16 kHz：

| 版本 | 停顿段 | 逐块 5–16 kHz 改动（中位 / P90 / 最大） | 高频上升的块 |
| --- | --- | --- | --- |
| 官方默认（`atten_lim=100`） | −61.3 dBFS | −1.3 / −0.3 / **−0.2 dB** | **0 / 230** |
| 官方 `atten_lim=24` | −60.2 dBFS | −1.2 / −0.3 / **−0.1 dB** | **0 / 230** |
| **我们（修复前）** | −59.3 dBFS | −4.6 / +6.9 / **+24.0 dB** | **63 / 230** |

降噪量相当，但我们在语音块上凭空加了一层最多 +24 dB 的高频 —— 听感就是「发毛、嘶嘶」。**这不是模型的问题，是我们自己的接线**：每个降噪任务里多调了 `model.init()` + `df_state.reset()` + `init_norm_states()`（官方从不调用），这三行会清掉模型内部的滚动缓冲与归一化状态，模型要再过 `df_order` 帧（5 帧 = 2400 采样）才填回来。同时它把真实算法延迟从 480 变成 2880，而当时 `enhance_samples` 里恰好按 2880 补偿 —— **两个错误互相抵消**，所以自检从 0.1.59 起一直报「滞后 0 采样、ρ(0) ≈ 0.99」，把问题盖了三个版本。0.1.59 那条「复用同一个模型会让输出每次多漂 `df_order` 帧」的实测也因此被误读成「模型固有延迟」，进而写出「官方公式漏了 `df_order`」这个错误结论。

**改法。** ① 删掉那三行（`run_denoise_job` 与两个自检里都不再调用）；② 延迟公式改回与官方一致：`fft_size − hop_size + lookahead × hop_size` = **480 采样（10 ms）**。

**验证。** 六档全部「滞后 0 采样」；中档复跑与首跑逐采样一致（断言）；且修复后与官方输出的逐块 5–16 kHz 统计**完全一致**（中位 −1.2 / P90 −0.3 / 最大值 −0.1 dB / 0 个块上升），原始波形互相关的 ρ 也同为 0.9291。**意外收获**：档位阶梯恢复了（见上一段），说明 0.1.60 关于「档位空转」的结论同样是这个 bug 造成的假象，已一并更正。

**方法论。** 这次的教训是：**自检报「对齐正确」不等于实现正确** —— 两个错误相抵时它照样报 0。以后怀疑降噪时，除了自检，**一定要拿官方参考实现做同素材同参数的对照**（改 `Cargo.toml` 的 `df` features 加上 `"bin", "wav-utils", "transforms"` 就能 `cargo build -p deep_filter --bin deep-filter`，跑完记得把 features 与 `Cargo.lock` 还原）。

### 响度标准化

工具栏第二排末尾的「响度标准化」（快捷键 `⇧L`）已从"波形整形压峰 + 静态增益 + 末端兜底"改成**静态增益 + 前瞻真峰值限幅**，只改增益、不动波形：4 倍过采样重建真峰值 → 求该点所需衰减 → 5 ms 前瞻窗口取最小值（波峰到来之前增益就已经降下去）→ 释放端按 80 dB/s 限速平滑 → 乘到信号上。旧实现逐采样改写波形形状，必然产生谐波（听感发毛），且过冲一大就压不回 ceiling，最后仍靠导出时硬 clamp 兜底——那才是真正削波的来源，现在这条路没了。

增益与限幅互相影响（限幅会拉低响度，ceiling 又要按实测真峰值收紧），所以跑「渲染 → 重测 → 修正」最多 4 轮，直到响度进 0.3 dB 容差且真峰值不过线。响度按成片口径测量（切除区间不计）。

目标响度在设置里可选或手填：播客 -19（**默认**）、广播 -23、短视频 -14，手动范围 -30 ~ -6。注意口径：BS.1770 对单声道是按单通道权重量测的，所以同一个单声道文件在立体声系统里回放会比读数响 3 dB——这就是「单声道 Apple Podcasts -19 / 立体声 -16」那 3 dB 的来源；-23 是 EBU R128 广播标准（美国 ATSC A/85 为 -24 LKFS），对单声道口播偏保守，归一完会明显觉得小声（0.1.49 把默认从 -23 改到 -19）。真峰值上限按导出格式分档：WAV 按播客规范 -1 dBTP，MP3 因为有损编码还会过冲约 0.5 dB，压到 -1.5 dBTP。

完成后播报响度前后值、真峰值与限幅衰减量；限幅衰减超过 6 dB 会提示"限幅较深，可换更轻的档位或调低响度目标"。处理前留一份缓冲快照，工具栏会出现「撤销响度」按钮（Ctrl+Z 只管区间编辑，回不到这一步）；降噪或换文件后快照失效，避免回退时把降噪成果一起吞掉。它是派生操作，不修改原始音频，「恢复原始（B）」仍然有效。这一步与「压缩」链路末尾那次归一用的是同一套算法，所以压过之后再点它通常不会有明显变化。

## 一键语音优化链（「压缩」按钮，0.1.49）

工具栏的「压缩」从 0.1.49 起不再只是压缩，而是一条固定顺序的链路：**高通 80 Hz → 向下扩展 → 软拐点压缩 → 响度标准化**。前三步只改增益或做线性滤波，真峰值限幅在整条链里只出现一次，就在末尾的归一里。

* **档位＝目标跨度**：轻 8 / 中 5 / 强 3 dB，回答「想把这段录音的动态收窄到多窄」。阈值与压缩比不预设，由素材自身反推——逐 100 ms 算块电平、用**锚在噪声底上的门限**（噪声底 + 14 dB）剔掉气口与停顿，取 P10 / P90 得到输入跨度，压缩比 ≈ 跨度 ÷ 目标（封顶 4:1）、阈值落在 P10 附近（6 dB 软拐点）。门限**锚噪声底、不锚中位数**（0.1.60 改）：中位数会随压缩移动，锚在它上面会让压缩前后测的不是同一批块，实测「输出跨度」反而比输入更大（播报过「跨度 19.2 dB → 24.5 dB」）。**跨度是相对量**：麦克风增益整体平移时 P10 / P90 同步平移、差值不变，所以拧大拧小、甚至换一支灵敏度完全不同的麦克风，都不改变判定（早期写死阈值的版本一漂就压空或压不动）。
* **高通**：二阶 Butterworth 80 Hz，削掉隆隆声、桌面震动与近讲低频堆积。线性滤波，不产生谐波。
* **向下扩展**：在停顿与气口处按住底噪，避免归一抬响度时把底噪一起抬起来。阈值夹在「噪声底」与「语音 P10」之间（`min(噪声底 + 6, P10 − 3)` dB），2:1、最多 12 dB。两条自动跳过：底噪比语音有效电平低 30 dB 以上（信噪比已经够好）、噪声底与语音挨得太近夹不出余量。用扩展而不是硬门，是因为硬门切气口很难听。
* **不补偿**：0.1.46 的「按最深处补偿」在 0.1.49 删除。它是静态抬全段、抬完由限幅器收拾，波形会变成平顶香肠；更关键的是末尾归一按实测 LUFS 重算增益，会把这份补偿在数学上抵消——收益归零，限幅留下的增益包络却不可逆。**「峰值没变小」改由「目标 LUFS + 限幅上限」共同保证**，播报里如实给最终真峰值与限幅衰减，用户自己就能确认峰顶到没顶到上限。
* **所有电平统计只算保留区间**，与响度测量同口径。早先统计含已切除区间，切掉一段响噪音会把 P90 撑大、压缩比高估，对成片压过头。
* **降噪不进链**：DeepFilterNet3 是异步工作线程（要加载模型、可取消、耗时几十秒），塞进来会把秒级按钮变成黑箱；它保持独立步骤，链路只在按钮旁给「建议先降噪」的灰字提示（未确认降噪时显示），残余底噪由扩展器兜。
* **撤销两级**：「撤销压缩」整体回退到链路之前（含链路里那次归一），「撤销响度」只退归一那一步、保留链路结果。重复点「压缩」是换档位，永远从链路之前的基准重算，不会叠加；换档位后此前单独做过的那次响度归一会失效并被自动重算，不提示用户重新点。
* **播报一次给完**：链路各步、实测输入跨度 → 输出跨度、生成的阈值与压缩比、成片有效电平、平均压掉多少 dB，以及响度前 → 后、最终真峰值与限幅衰减。素材本来就稳会提示「无需压缩」但仍照常做归一；压缩比顶到 4:1 仍压不够也会如实说明；有效块不足 2 秒（短录音）走兜底参数「成片有效电平 − 3 dB、压缩比 2:1」。

链路顺序、自适应口径与播报的约束写在 AGENTS.md 硬约束 13，改之前先读那一节。尚未做的部分：齿音抑制（de-esser，电容麦近讲时的 5 ~ 8 kHz 齿音）与录音端的输入电平主动引导（实时提示落在 -12 ~ -6 dBFS 目标带）。

### 跨度门限锚点修复（0.1.60）

**遇到的问题。** 有用户反馈点完「压缩」后，播报里出现「跨度 19.2 dB → 24.5 dB」——**压缩之后动态反而变大了**；同时压缩感偏重（平均压掉 11 ~ 13.6 dB），听感上底噪比压缩前更明显。

**排查过程。** 拿一条真实录出来的口播素材（48 kHz 单声道 26.99 s，块电平 P10 −46.76 / 中位数 −27.99，即信噪比 18.8 dB）先把 `analyseSource` / `planExpander` / `planCompression` 的口径用外部脚本原样复刻一遍，算出「按这套代码，播报应当读到什么」。结果是六项**全部对不上**：预测扩展器「跳过(no-headroom)」而实际「生效」、预测输入跨度 25.2 而实际 19.2、预测阈值 −45 而实际 −39、预测压缩比 4.0 而实际 3.8、预测输出跨度 15.2 而实际 24.5。

关键线索是错法的**形状**：中位数与 P90 都对得上（差 0.6 dB），只有**噪声底低了 6 dB、P10 高了 6 dB**。这个形状指向「当时那个缓冲已经降过噪」—— 用降噪（轻档）的输出重跑复刻，六项**同时对上**（噪声底 −53.99 对 −53、阈值 −38.03 对 −39、跨度 18.55 对 19.2、压缩比 3.71 对 3.8、扩展器生效阈值 −47.99 对 −47、输出跨度 21.79 对 24.5、同方向）。复刻可信之后，「输出跨度反而变大」也被稳定复现。

**根因。** `analyseSource` 剔除气口的门限原来是「**本段块电平中位数** − 20 dB」，而压缩会移动中位数：

* **压缩后仍可量、但已经不可比**：压缩把语音压低十几 dB → 中位数下降 → 门限跟着下降 → 原本被剔掉的气口重新进入统计 → 输出 P10 掉到气口电平 → 实测「输出跨度」比输入更大。播报里那个自相矛盾的数字就是这么来的。
* **未降噪的中等信噪比素材上更糟**：气口只比中位数低约 19 dB，落不进 −20 dB 的门限，气口于是被算成语音，**P10 直接等于气口电平** → 压缩阈值落在气口上、压缩比一路顶到 4:1；同时扩展器的 `no-headroom` 判据（`P10 − 噪声底 > 9 dB`）**恒不成立而永久跳过**（实测 headroom 只有 1.59 dB，判据门槛是 9 dB）。
* 这套口径之所以「看起来能跑」，是因为默认工作流是「建议先降噪」—— 降噪把气口拉低约 7 dB，正好让气口落进门限之下。也就是说它**依赖上游先降噪才对**，而这个依赖藏在灰字提示里。

**改法（为什么锚噪声底）。** 噪声底是气口的电平，压缩在气口处的增益≈0，所以它**不随压缩移动**；它同时是绝对量，麦克风增益整体平移时同步平移、门限跟着平移，判定不变。锚它同时解决上面两个问题：

* 门限改为 `噪声底 + SPEECH_GATE_MARGIN_DB`（14 dB），并**夹在「中位数」以下** —— 素材全程连续说话、根本没有气口时，块电平的 P10 不是噪声底而是最轻的语音，此时门限高过中位数就会把一半动态当成噪声切掉。
* 量压缩输出时把**输入的**噪声底当锚传进去（`analyseSource` 的第三个参数），前后测的才是同一批块，播报的「跨度 A → B」才可比。
* 14 dB 这个余量不是随手取的：它必须**显著高于**扩展器阈值相对噪声底的上移量（`EXPANDER_KNEE_DB = 6`），否则气口又会被算回语音侧。

**验证（27 s 整段，中档）**：

| 输入 | 旧口径输入跨度 | 新口径输入跨度 | 新口径输出跨度 | 扩展器 | 压缩比 |
| --- | --- | --- | --- | --- | --- |
| 原始文件 | 25.20（气口混进统计） | 10.77 | **7.01**（收窄） | 跳过(no-headroom) → **生效，阈值 −40.8** | 4.00 顶上限 → **2.15** |
| 已降噪（轻） | 18.55 | 14.81 | **9.21**（旧口径是 18.55 → 21.79，反而变宽） | 生效 | 3.71 → 2.96 |
| 已降噪（中） | 17.46 | 17.21 | 10.75 | 跳过(clean) | 3.44 |
| 已降噪（强） | 17.43 | 17.10 | 11.58 | 跳过(clean) | 3.42 |

四种输入下「输出跨度」一律小于「输入跨度」；原始文件不再顶 4:1；扩展器不再误跳过。`tsc --noEmit` 与 `cargo check` 通过。

**这次没用上的东西（下次注意）。** 复刻只覆盖到压缩为止，**归一与真峰值限幅两段没进模型**，所以播报里「响度前 → 后 / 最终真峰值 / 限幅衰减」这三个数至今没有独立校验手段，是盲区。

## 未结项（0.1.60 留待彻查）

两条尾巴的影响面（2026-09-26 用 `沧海-20260926-115728.wav` 实测）：

| 环节 | 受「余量」影响 | 受「HF 伪影」影响 |
| --- | --- | --- |
| 录音原文件 | 无 | 无 |
| 降噪 | 无 | **伪影就是在这一步产生的** |
| 静音检测 | 无 | 几乎无（宽带电平没变，有的还更安静） |
| 向下扩展 | **无**（阈值恒为「噪声底 + 6 dB」，与余量无关） | 无 |
| 压缩 | **就这一环**（阈值 / 跨度 / 压缩比 / 压掉量） | 无 |
| 响度归一 | 无 | 可忽略（2 个 100 ms 块的高频能量，对整段 LUFS 是 0.0X dB 量级） |
| 真峰值限幅 | 无 | 无（伪影在 −41 ~ −55 dBFS，离 −1 dBTP 的 ceiling 差 30 dB 以上） |
| 转录 SenseVoice | 无 | 可忽略（**推断**：它按 16 kHz 取特征，且伪影只出现在有语音的块）— 未实测 |
| 导出成品 | 听感上就是「压多少」 | 偶发的「嘶 / 毛刺」 |

### 1. `SPEECH_GATE_MARGIN_DB = 14` 是拍的起点，得用耳朵定

这是上面那个修复里唯一「拍」出来的数，它决定「多安静的块算气口」，直接决定压缩阈值落点。

* **偏大**：把最轻的语音也当成气口剔掉。这条素材上 14 dB 剔掉了 40 % 的块（162 / 269），很可能连最轻的词尾一起剔了 —— 后果是跨度量小、阈值偏高，**轻的句子不会被带上来**。
* **偏小**：门限压不住气口，又回到「压缩阈值落在气口上、压缩比顶 4:1、扩展器恒跳过」的老问题。硬下限是必须大于扩展器的 `no-headroom` 门槛 9 dB。
* **怎么判**：挑一条起伏明显的素材，用「压缩：中」跑一遍，重点听**最轻的那几句有没有被带上来**。觉得「没带上来」就把 14 往下调（12 → 10）；觉得底噪又被压得跳出来就往上调。
* **落点**：`src/utils/compression.ts` 的 `SPEECH_GATE_MARGIN_DB`。

**实测敏感度（原始文件整段、中档，噪声底 −46.76 dBFS）：**

| 余量 | 门限 | P10 | 输入跨度 | 有效块 | 压缩比 | 输出跨度 | 平均压掉 | 压缩后「语音 − 停顿」间距 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 8 | −38.8 | −34.2 | 14.66 | 192/269 | 2.93 | 8.75 | 7.31 dB | — |
| 10 | −36.8 | −33.1 | 13.62 | 184/269 | 2.72 | 7.88 | 6.44 dB | 25.01（比原始掉 3.4） |
| 12 | −34.8 | −32.3 | 12.86 | 178/269 | 2.57 | 7.36 | 5.83 dB | — |
| **14** | **−32.8** | **−30.1** | **10.77** | **162/269** | **2.15** | **7.01** | **4.13 dB** | **27.26（掉 1.2）** |
| 16 | −30.8 | −28.1 | 8.90 | 152/269 | 1.78 | 6.46 | 2.72 dB | — |
| 18 | −28.8 | −27.3 | 8.17 | 138/269 | 1.63 | 6.83 | 2.16 dB | 29.28（反升 0.8） |
| 22 | −28.0 | −27.1 | 7.99 | 135/269 | 1.60 | 6.92 | 2.03 dB | — |

（「语音 − 停顿」间距按本次脚本的固定块索引定义：语音取宽带 top 25 %、停顿取 bottom 10 %；原始值为 28.45 dB。）

三条判读：

1. **它不产生失真，只改「压多少」**。`applyCompression` 的增益恒 ≤ 0（只衰减、从不抬升），所以门限高低不可能引入失真或改音色 —— 上面那句「14 dB 剔掉了 40 % 的块」**不是删音频**，只是换了统计样本集。
2. **全程平滑，没有悬崖**：8 → 22 之间压缩比 2.93 → 1.60、平均压掉 7.31 → 2.03 dB，单调连续。所以**拍错不会翻车**，只是压多压少。
3. **真正关键是「锚点」，不是余量**：同一档在旧口径（锚中位数）下「压缩后间距」要掉 12.9 dB，改锚噪声底之后余量 10 掉 3.4、余量 14 掉 1.2、余量 18 反升 0.8 —— 「底噪会不会被压得跳出来」这一项**已经被锚点那一刀解决了九成**。另外「扩展器阈值」一列在 8 ~ 22 全档恒为 −40.76 dBFS（因为余量 > 9 时它恒取「噪声底 + 6」），所以**调这个数只用管压缩侧**，不牵动别的模块。

可用区间约 **12 ~ 18**；**余量 ≥ 20 时门限会被「夹在中位数以下」那个保护接管**（有效块掉到 135、只剩一半），那种值不要用。

### 2. ~~降噪会在个别块上凭空抬高 5–16 kHz（偶发伪影）~~ —— **0.1.61 已修复，关闭**

> **结论更正**：根因不是模型、也不是 `atten_lim`，而是每个降噪任务里多调的那套 `init()` / `reset()` / `init_norm_states()`（见「降噪接线修复（0.1.61）」）。修复后同素材同档位的逐块 5–16 kHz 改动为「中位 −1.2 / P90 −0.3 / 最大值 −0.1 dB / 0 个块上升」，与官方参考实现一致。
> **下面整节保留为当时的现场记录**，可作前后对照 —— 也提醒一件事：同一个现象换一种统计口径会给出不同画面，当时「逐块与输入相减」看到 63/230 块上升，而「块相对邻块的孤立度」看到的是「和原声差不多」（因为原声自带的齿音峰就有 50 个），两个都要看。

**现象（现场记录）。** 在真实口播素材上按 100 ms 分块、逐块比对降噪输出与输入在 5–16 kHz 的电平：中位数是 −3.0 dB（多数块在降，停顿块最高降 19 dB），但**有 46 ~ 50 个块（约 1/3）反而升高，最大 +24 dB**；具体到停顿块，15 个里有 2 个从 −74 dB 跳到 −55 dB 左右。表现为偶发、窄时（约 100 ms）、宽带的爆点。

**已排除。** 与 `atten_lim` 无关：把档位开到「完全不掺回」（`set_atten_lim(100)`，即 `None`）时那两个块依然 +19.8 dB，所以不是「掺回原始含噪谱」造成的。

**未查。** 是模型/合成侧的问题（帧率门控 / 音乐噪声？）还是我们接线的问题（分块边界？延迟补偿的尾部补零？tract 算子？）。

**为什么值得查。** 它是「降噪后偶发嘶声 / 毛刺」的候选来源；而且它**不是**当初去嘶声想治的那种「5 kHz 以上稳定漏出来的嘶声」，一个静态的高频 shelving 治不了偶发爆点，只会把语音整体削闷 —— 要处理应当针对**离群块**而不是全段。

**怎么复现。** 先跑 `$env:GAP_GONE_DENOISE_TEST_WAV="<某个真实口播 wav>"; cargo test --lib diagnostic_denoise_alignment -- --nocapture`，结果会落盘到 `temp/gap-gone-dfntest/`；再按 100 ms 块逐块比对输入与输出的 5–16 kHz 电平。素材截取长度用 `GAP_GONE_DENOISE_TEST_SECONDS`（默认 15 秒，`0` 表示整段）。

**排查方向建议。** ① 把同一段素材从头、尾各截 5 秒分别跑，看爆点是否跟着分块边界走（若是，问题在我们接线）；② 跑一段纯噪声，看模型对非语音的反应（DeepFilterNet 是语音模型，非语音段是它的弱区）。

**另记一条方法论。** 分带电平**不能只对片段求和** —— 最初把 15 个停顿块的功率求和，得出「降噪后高频整体 +9 dB」的结论，拆成逐块才发现是其中 2 个块贡献的，其余 13 个都在降。

**绝对电平（前 15 s、强档、与输入同块索引）。** 参照量是**语音块（宽带 top 25 %，38 块）的 5–16 kHz 电平中位数 −51.37 dBFS**（P75 −48.14）：

| 块号 | 输入宽带 | 输出宽带 | 宽带差 | 输入 5–16k | 输出 5–16k | HF 差 | 输入里 HF 比宽带低 | 输出里 HF 比宽带低 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 5（停顿） | −47.5 | −53.4 | **−6.0** | −74.4 | −54.7 | +19.6 | 26.9 dB | **1.3 dB** |
| 56（停顿） | −47.2 | −50.7 | **−3.5** | −75.3 | −59.7 | +15.6 | 28.1 dB | 9.0 dB |
| 65（语音） | −27.7 | −28.5 | −0.8 | −67.6 | −52.2 | +15.4 | 39.9 dB | 23.7 dB |
| 69（语音） | −30.0 | −30.6 | −0.6 | −66.0 | −45.6 | +20.5 | 36.0 dB | 15.0 dB |
| 93（语音） | −24.5 | −25.9 | −1.4 | −65.0 | −41.0 | +24.0 | 40.5 dB | 15.1 dB |

三条判读：

1. **宽带电平上看不见它**：出现伪影的块，宽带变化只有 −0.6 ~ −6.0 dB，多数是**变安静**。所以电平表、LUFS、响度归一、真峰值限幅、静音检测都感知不到 —— 它不会让整段变吵，也不会触发任何一层的判定。
2. **它改的是频谱形状**：输入里 5–16 kHz 比宽带低 27 ~ 40 dB（高频很干净），输出里变成低 1 ~ 24 dB；停顿块 5 的残余**几乎只剩高频**（只低 1.3 dB）。
3. **不只在停顿里，语音段也有**：上表 5 块里 3 块是语音块（输入宽带 −24 ~ −30），它们输出的 5–16 kHz 落在 −41 ~ −52，**比语音自己的高频中位数（−51.4）高 10 dB 上下**。5–16 kHz 本来就是语音能量极低的区域、掩蔽很弱 —— 这层新加的高频大概率听得见。

**规模。** 前 15 秒的 150 个 100 ms 块里，HF 升高的有 46 ~ 48 个（约 1/3），但多数幅度不大（中位数 −3.1 dB、P90 +6.1 dB）；**+15 dB 以上的极端点在 15 秒里至少 5 个**，约合每 1 ~ 2 秒一次可感的抬升。

**数据来源与局限。** 全部来自**同一条素材**、且只跑了**前 15 秒**（`diagnostic_denoise_alignment` 的默认窗口），整段 27 秒的出现率未测；换素材可能不同。原始数据是 DFN 输出落盘文件（`temp/gap-gone-dfntest/denoise-*.wav`）与输入按同一块索引逐块比对，复现只要重跑那个自检 + 一段逐块分带的脚本。

**下一步不是查机制，而是先用耳朵确认存在性。** 拿这条素材跑一遍**轻档降噪**，在波形上跳到 **6.5 s 与 9.3 s 附近**（对应块 65 / 93），反复听那两下有没有「嘶 / 呲」的毛刺。听不出来就不必投入排查；听得出来再按上面的排查方向走。

## 语音转录（SenseVoice）

桌面端内置 SenseVoice-Small（int8）本地离线转录，经 sherpa-onnx 官方 Rust binding 接入，无需联网、无需 API key。支持中/英/日/韩/粤语自动识别，但**不做说话人分离**（多人对话不区分说话人）。

点击工具栏「转录文字」（快捷键 `T`）开始：首次使用会先下载模型（约 230MB，显示下载进度），之后转录全程本地完成；首次转录还会顺带下载标点恢复模型（约 75MB，一次性，失败自动降级为无标点输出）。模型目录可在「设置」中自定义（Windows 默认在仓库内的 `D:\Code\Github\gap-gone\models\sense-voice`，其他平台默认 `~/models/sense-voice`，旁边有「打开目录」按钮直达；0.1.6 起默认目录从应用数据目录改到用户主目录，旧位置已有模型的需手动迁移）；「设置」中的「录音确定编辑后自动转录」默认开启。转录结果以两种形态呈现：**每行波形下方的逐字词带**（字按时间戳与波形横向对齐，正在播放的字高亮，被切除区间覆盖的字划线置灰，点击对应位置即跳转——词带是"瞄准镜"，切除/恢复仍用拖拽工具画区间）和**波形下方的句子面板**（点击句子跳转，播放句高亮跟随）。句子文本自动补全标点；`.srt` 字幕按标点分句（多句长条拆分、碎句合并），`.txt` 文字稿自动分句分段（停顿换段 + 句末收段），两者均可导出文件或一键复制到剪贴板。转录锚定原始完整时间轴，切除/恢复不需要重跑。

### 时间戳精度说明

SenseVoice 是 CTC 架构，输出字级（中文）/子词级（英文）时间戳，分辨率约 60ms，典型偏差 ±100\~200ms。适合"点字定位再听辨剪辑"，不适合当作精确切割点直接下刀。

### 模型文件位置与手动下载

模型目录（Windows 默认为仓库内的 `D:\Code\Github\gap-gone\models\sense-voice\`，其他平台为 `~/models/sense-voice/`；0.1.6 之前默认在应用数据目录）：

目录中需要两个文件（应用会自动检测，齐全即跳过下载）：

| 文件                | 大小       | 说明                         |
| ----------------- | -------- | -------------------------- |
| `model.int8.onnx` | 约 229 MB | SenseVoice-Small int8 量化模型 |
| `tokens.txt`      | 约 309 KB | 词表                         |

**手动下载（国内网络推荐，走 hf-mirror 镜像）。Windows PowerShell：**

```powershell
$dir = "D:\Code\Github\gap-gone\models\sense-voice"
New-Item -ItemType Directory -Force $dir
curl.exe -L -o "$dir\model.int8.onnx" "https://hf-mirror.com/csukuangfj/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2025-09-09/resolve/main/model.int8.onnx"
curl.exe -L -o "$dir\tokens.txt" "https://hf-mirror.com/csukuangfj/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2025-09-09/resolve/main/tokens.txt"
```

**macOS 终端：**

```bash
dir="$HOME/models/sense-voice"
mkdir -p "$dir"
curl -L -o "$dir/model.int8.onnx" "https://hf-mirror.com/csukuangfj/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2025-09-09/resolve/main/model.int8.onnx"
curl -L -o "$dir/tokens.txt" "https://hf-mirror.com/csukuangfj/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-int8-2025-09-09/resolve/main/tokens.txt"
```

海外网络把 `hf-mirror.com` 换成 `huggingface.co` 即可（URL 其余部分不变）。也可以用浏览器打开上述链接下载，再把两个文件放进模型目录。

### 实现要点

* `src-tauri/src/transcribe.rs`：识别器常驻 `gap-gone-transcribe` 工作线程（模型只加载一次）；整段音频按 \~60s 在低能量点切块，块间汇报真实进度并检查取消标记；词级 token 聚合成句子（滤除 `<|...|>` 元标签），切分策略：标点优先，超过 8s 未遇标点则在 >0.4s 的词间停顿处下刀，15s 硬切——口语化演讲常常整段无标点，纯标点切分会产生分钟级超长字幕段。

* 下载：HuggingFace 主站 → hf-mirror 镜像依次尝试，先写 `.partial` 再改名，中途取消/断网不会留下半个文件被误判为已就绪。

* 前端：`src/utils/transcribe.ts`（16 kHz 重采样 + 临时文件传参 + SRT/TXT 导出）、`src/components/TranscriptPanel.tsx`（句子面板）。

## 导出

默认导出 WAV（单声道、16-bit PCM；**采样率跟随录音设备**，48 kHz 设备上才是 48 kHz —— 见下节的位深与采样率评估），可在设置中切换为 MP3（128 kbps CBR 单声道，`@breezystack/lamejs` 前端编码）并调整码率（96 / 128 / 192 kbps）。默认文件名带有精确到秒的日期时间前缀，例如 `20260823-233600-edited-audio.wav`，减少意外覆盖。MP3 编码为纯 JS 实现，按块分批让出主线程，长录音导出期间界面不冻结。设置跨启动保留（`src/utils/settings.ts`，localStorage）。

体积参考（单声道）：WAV 约 5.76 MB/分钟；MP3 128 kbps 约 0.9 MB/分钟、96 kbps 约 0.7 MB/分钟、192 kbps 约 1.4 MB/分钟。

### 导出位深与采样率评估（2026-09-26，结论已定、暂不实施）

背景：行业里常推荐交付「48 kHz / 24-bit」。当前两处都是 16-bit，评估如下。

**两处 16-bit，上游那处才是真正的损失点。**

* **录音落盘**（`src-tauri/src/lib.rs` 的 `WavSpec`）：`bits_per_sample: 16`、`SampleFormat::Int`，写入线程里做 `*sample * i16::MAX → i16`。而录音回调本身支持 F32 / I16 / U16 三种设备格式，**WASAPI 共享模式与 CoreAudio 通常给的是 float32** —— 设备给的浮点采样在写入线程里就被砍成 16-bit。这是整条链路上第一次、也是不必要的一次有损。
* **导出**（`src/utils/exportUtils.ts` 的 `bufferToWav`）：16-bit 硬编码在源码里（注释自己写着 "hardcoded in this simple converter"）。

**但「改成 24-bit」的听感收益是零。** 量化噪声账（以 `沧海-20260926-115728.wav` 的实测值为参照）：

| 位深 | 量化噪声 RMS | 与素材底噪（−46.76 dBFS）的关系 |
| --- | --- | --- |
| 16-bit | ≈ −101 dBFS | 比底噪低 **54 dB** |
| 24-bit | ≈ −149 dBFS | 比底噪低 102 dB |

16-bit 的量化噪声比底噪低 54 dB、比 10–16 kHz 段的噪声底（−80 dBFS）还低 27 dB —— 早就被掩蔽了。所以「推荐 24-bit / 48 kHz」里的“推荐”说的是**工作格式与交接格式**，不是听感：① 交给视频后期（Premiere / 达芬奇 / FCP）或 DAW 时，48 kHz / 24-bit 是通用工作格式，省掉对方一次转换；② 反复「导入 → 处理 → 导出」往返时不累积量化误差。

**两处比位深更值得动的地方。**

* **采样率不一定真是 48 kHz**：导出沿用 `buffer.sampleRate`，也就是**录音设备的原生率**（`buildExportBuffer` 不做重采样）。Mac 上不少 USB 麦是 44.1 kHz，导出来就是 44.1 kHz。要严格 48 kHz，得在导出时加一道重采样。
* **16-bit 转换是「向零截断」不是四舍五入**：`bufferToWav` 里 `sample | 0` 会把 12.7 变成 12、−12.7 变成 −12，量化误差从 ±0.5 LSB 变成 0 ~ 1 LSB 且带符号相关的偏移。改成 `Math.round` 即可 —— 同样听不出来，但属于白送的精度。

**实施时的硬约束（真要动再看这段）。** `bufferToWav` **不能整体改**，它被三处共用：播放源（`App.tsx` 的 `syncMediaSource`）、转录输入（`transcribe.ts`，那里需要的就是 16 kHz / 16-bit）、以及导出。所以要给导出**单独写一个 24-bit PCM 编码器**（`WAVE_FORMAT_PCM` + `bits_per_sample = 24`，每采样 3 字节小端），并在 `settings.ts` 里加一个「导出位深：16 / 24」的选项（已有 `exportFormat` / `exportBitrate` 的先例）。**默认值建议 24** —— 三种下游都要照顾时，24-bit 是唯一"谁都能吃"的选择：视频后期与 DAW 要它，播客平台会自行转码所以多出的体积无害，而且用户可以切回 16-bit。

**为什么暂不实施。** 听感收益为零，而这是一个会改文件体积、加设置项、还要同步 README 的改动；等真有「要交给剪辑或归档」的需求时再做，改动面已在上面列清楚。

## 构建与运行产物

`pnpm build` 只生成前端静态资源，输出目录为 `dist/`。由于构建页面使用根路径资源，不建议直接双击 `dist/index.html`；查看生产前端应运行：

```bash
pnpm preview --host 127.0.0.1
```

开发服务器默认使用 `http://127.0.0.1:1420/`。如果该地址仍显示旧代码，需要停止旧的 Vite 进程后重新运行 `pnpm dev --host 127.0.0.1`。

在 `pnpm dev` 或 `pnpm preview` 的浏览器环境中导出 WAV 会直接触发浏览器下载；Tauri 桌面应用则使用系统保存对话框。

`pnpm tauri build` 会生成桌面应用安装产物，通常位于 `src-tauri/target/release/bundle/`。macOS 应用位于 `macos/`，磁盘镜像位于 `dmg/`；具体产物取决于当前操作系统和 Tauri 打包配置。

## 视频支持（0.1.62 移植）

### 为什么不是合并分支

视频能力原先做在 `video-support` 分支（提交 `e805a11`，版本 0.1.29），而它是从 `391f96f` 分出去的孤岛 —— 之后主开发线又走了 32 个提交（0.1.30 ~ 0.1.61）。**直接 merge 或 cherry-pick 那个提交会把 `Cargo.toml`（退回 `default-model`、删掉 tract 锁版本与 `profile.dev`）、`settings.ts`（删掉静音阈值 / 响度目标 / 压缩档位）、`lufs.ts`、`compression.ts`、`docs.md`、`README.md` 一起拉回 0.1.29。** 所以做法是以当前分支为基准**只移植视频增量**：新增 `src-tauri/src/video.rs`、`src/components/VideoPreview.tsx`，配置加 `protocol-asset` 与 asset CSP，`settings.ts` 只加 `gap-gone-ffmpeg-path`，App.tsx 的视频逻辑按新播放引擎（媒体元素，见硬约束 10）重写。

### 移植时修掉的两处

1. **copy 档每个片段都漏了 `-t duration`**（原实现在 `!fastcopy` 时才下发）。`-ss` 作为输入参数只决定起点，不带 `-t` 的片段会从切点一路延伸到片尾。本机 ffmpeg 复刻（29.2 s 的素材）：`-ss 1.000 -c copy` 产出 28.13 s，加 `-t 2` 才是 2.016 s。现在 `-t` 无条件下发，copy 与重编码两条路都覆盖。
2. **抽音轨写的是 16-bit PCM**（原注释的理由是「避免 32-bit float 让临时文件翻倍」）。这与硬约束 3 的结论相反：口播有效电平常在 −30 dBFS，16-bit 下底噪那段只剩个位数 bit，而降噪的抑制决策正依赖对底噪的准确估计。现在改 `pcm_f32le`，前端用 `floatWavToBuffer` 自解析读回（ffmpeg 写 float 用 40 字节 WAVE_FORMAT_EXTENSIBLE，自解析按 chunk 扫，两种头都认）。

### 本次定下的口径

* **导出默认「无损快速」（`fastcopy`）**，画面 `-c:v copy`，未处理音频时音轨 `-c:a copy` 原样保留；`reencode` 作为精确档备选。语义与代价写进 `CONTEXT.md` 的 VideoExportVariant。
* **音轨按源文件的原始采样率与声道数抽出**，不下混、不重采样；波形与播放侧电平改成按声道下混显示（原先只读左声道，与按所有声道统计的处理链口径不一致，也和整段峰值读数对不上）。
* **asset 协议只放行当前导入的单个视频文件**：静态 `scope` 留空，导入后调 `allow_video_asset`，换素材或关闭时 `forbid_video_asset` 收回。
* **字幕这轮不烧录**，只做「按成片时间轴重映射的 `.srt`」随视频导出（`mapRangeToKept` + `buildSrt`）。烧录要等 libass 能力探测与中文字体方案。
* **音画起点不一致的视频直接拒绝导入**：抽出的音轨从 0 起算，源文件音轨若比画面晚 50 ms 以上，编辑期与导出会整体错位。

### copy 档的切点与 A/V 对齐：实测结论与修法（2026-09-26）

素材：`F:\OBS_videos\2606\2026-09-26 16-35-31.mp4`（OBS 录的 H.264 1080p30 恒定帧率、31.1 s、AAC 立体声 48 kHz、**fragmented MP4**、关键帧只有 4 个：0 / 8.333333 / 16.666667 / 25）。

**根因是我们自己加的 `-avoid_negative_ts make_zero`。** 输入侧 `-ss` 会把请求点之前的内容一起读进来（时间戳为负，充当解码参考帧），`make_zero` 把这些 pre-roll「扶正」保留下来，等效于把切点拉回文件开头：

| 命令（请求 `[5.000, 8.000]`） | 容器时长 | 首帧落在源的第几帧 |
| --- | --- | --- |
| `-ss 5.000` + `make_zero` | 8.000 s | 第 0 帧（0.000 s）✗ |
| `-ss 5.000` 不带 `make_zero` | 3.000 s | 第 150 帧（5.000 s）✓ |
| `-ss 8.333` 不带 `make_zero` | 1.667 s | 第 250 帧（8.333 s）✓ |

复现（建帧哈希表 → 反查产出首帧）：

```powershell
# 只对视频轨建帧表：第 N 行 = 源第 N 帧（30 fps 时 N/30 = 秒）
ffmpeg -v error -i in.mp4 -map 0:v:0 -f framemd5 - | Select-String '^\d+,'
# 切片（不带 make_zero），再取产出首帧哈希去表里反查
ffmpeg -y -ss 5.000 -i in.mp4 -map 0:v:0 -map 0:a:0 -c copy -t 3.000 out.mp4
ffmpeg -v error -i out.mp4 -frames:v 1 -f framemd5 -
```

**另一个坑：输入侧 seek 会把「到上一个关键帧为止」的数据整段写进文件。** 请求 3 秒的段，容器时长 3.0 秒、呈现 90 帧，但**物理上装了 240 个包 / 6.33 MB**（= 源 `[0,8)` 的全部数据），靠 edit list 把呈现起点裁到 5.0。这直接毁掉拼接 —— concat demuxer 不认 edit list，会把 pre-roll 一起拼进去：实测两段（1.5 s + 2.0 s）拼出来是 **10.886 秒 / 230 帧**的垃圾。

**修法**：fastcopy 档改成**输出侧 `-ss`**（放在 `-i` 之后）—— pre-roll 在 muxer 层被丢掉，段的物理包数 = 真实时长，concat 结果正确。实测两段（起点 0 与 16.666667，各 2.0 / 1.5 秒）：

| 项 | 结果 |
| --- | --- |
| 第 1 段 | 2.005 s / 60 帧 / 60 包 / 1.31 MB |
| 第 2 段 | 1.509 s / 45 帧 / 45 包 / 1.56 MB |
| concat | **3.505 s / 105 帧**（期望 3.5 / 105 ✓），切点处时间轴连续 |

代价是输出侧 seek **只能落在关键帧上**：`-ss` 落在 GOP 中间时视频会被整段丢掉（实测只剩 0.04 MB 音频）。所以 fastcopy 必须配合「把切点吸附到关键帧」，并由前端按 `probe_video` 返回的关键帧列表判定本次剪辑能否无损完成 —— 不行就自动降级到精确重编码并说明原因。

吸附时有两个精度陷阱：① 关键帧时间戳是 6 位小数（`16.666667`，真值 `16.6666666…`），**四舍五入抬高一点就会越过关键帧**，输出侧 seek 会丢掉整段画面（实测 `-ss 16.667` 得到无画面文件），所以前端把起点往回让 1 ms（`FASTCOPY_SEEK_EPSILON`）、Rust 侧再按 4 位小数格式化；② 吸附点不能退回到上一个保留区间的尾巴里，否则等于把已删掉的内容搬回来。

**音频要注意 priming 边数据（0.1.64 修正）**：这一节最初得出的「`-c:a copy` 在两条路上都安全」**只对没有 skip samples 前置边数据的素材成立** —— 手机录的 AAC 常带这种边数据（实测 0.859 秒），copy 进每一段后 concat 会把成片拼错位。现在 reencode 档的音轨一律重编码，详见下面「第三个坑」。

**顺带一个对创作流程的结论**：OBS 默认关键帧间隔（实测 250 帧 ≈ 8.3 秒）下，「无损快速」档只能按关键帧粗切，删静音这类精细剪辑做不了。把 OBS 的**关键帧间隔设为 1 秒**，无损快速档才有实用价值。**具体步骤与三个易踩点见 `README.md` 的视频一节**：这项在 OBS 的「简单」输出模式下根本看不到（必须先切到「高级」），而切到高级后编码器 / 录像路径 / 码率都换成另一套配置，不确认就会录到别的文件夹或用上 CPU 软编。

### 四类真实素材的实测状态（2026-09-26）

| 素材 | 容器/编码 | 关键帧间隔 | 导入（现有规则） | 无损快速 | 精确重编码 |
| --- | --- | --- | --- | --- | --- |
| OBS `2026-09-26 16-35-31.mp4` | fragmented MP4 / H.264 1080p30 | 8.333 s | ✅ | ⚠️ 只能按关键帧粗切 | ✅ |
| OBS `2026-09-26 16-46-11.mp4` | 普通 MP4 / H.264 1080p30 | 8.333 s | ✅ | ⚠️ 同上 | ✅ |
| OBS `2026-09-26 16-50-19.mp4` | 普通 MP4 / H.264 1080p30 | 8.333 s | ✅ | ⚠️ 同上 | ✅ |
| 手机 `PMR00223.mov` | 普通 MOV / **HEVC** 2560×1920 + **旋转 -180** | 0.933 s | ✅（0.1.64 放开） | ❌ 会静默丢帧 → 强制重编码 | ✅（转 H.264，方向正确） |
| OBS `2026-09-26 17-44-47.mp4`（改成 1 秒关键帧后录） | 普通 MP4 / H.264 1080p30，**含 B 帧** | **1.000 s（整秒网格）** | ✅ | ❌ 含 B 帧 → 强制重编码（0.1.65 起挡住） | ✅ |
| OBS `2026-09-26 17-54-37.mp4`（改回 Hardware 编码后录） | 普通 MP4 / H.264 1080p30，**无 B 帧** | **1.000 s（整秒网格）** | ✅ | **✅ 可用，端到端精确（0.1.66 起「安全吸附」）** | ✅ |

**「硬件编码 + 1 秒关键帧」这份就是目标形态**（改完 OBS 设置后录的，实测）：`has_b_frames=0`、关键帧 16 个 / 464 帧、间隔 1.000 秒且在整秒网格上。无损快速档端到端逐段精确 —— 切 `[0,5]` 与 `[10,15]`：每段 **150 包 / 首包 pts=0**，拼接后 **300 包 / 10.027 秒**（期望 300 / 10.0），解码无任何报错。**这是第一个「1 秒关键帧 + 无 B 帧」的组合**，说明 B 帧与关键帧间隔这两条限制各自独立成立：前者是 seek 的 dts 口径问题，后者只是精度与体积的取舍。OBS 侧确认到位：`[AdvOut] RecEncoder=h264_texture_amf` + `recordEncoder.json` = `{"keyint_sec":1}`（录像侧也设了，不只是直播侧）。**唯一遗留**：码率仍是 2748 kbps，比简单模式那三个（5.7 ~ 8.6 Mbps）低不少，建议在录像标签把质量控制提到 CQP 20 附近或 CBR 8000~10000 kbps。

**第二个 OBS 视频的体检**（用户按建议把关键帧间隔改成 1 秒后录的）：

- **设置确实生效**：24 个关键帧、间隔 1.000 秒、时间戳落在整秒网格上（0, 1, 2, …），共 719 帧 / 23.97 秒 —— 切点只要落在整秒上就能无损精确切。
- **但这次录像是 x264 软件编码，不是 AMD 硬件编码**。证据：`has_b_frames=2`（硬件那份是 0）、码率掉到 **2660 kbps**（前三个是 5.7 ~ 8.6 Mbps）、文件里能扫到 `x264` 签名而没有 `AMF`。原因是切到「高级」输出模式后录像编码器是空的（`RecEncoder=none`），OBS 用了直播那套的 x264；用户改的关键帧间隔也存在 `streamEncoder.json`（直播侧），录像只是**跟着直播编码器继承了它**。**要改回硬编必须在「录像」标签里显式选一次 AMD HW H.264，并确认录像侧自己的关键帧间隔也填了 1** —— 否则一旦录像编码器与直播分离，录像会退回自动（= 8.3 秒）。
- **录像路径也换了**：`[AdvOut] RecFilePath=F:\OBS_videos\2609`，与简单模式的 `2606` 不是同一个文件夹（新视频果然落在 2609）。

**「分段 MP4 是不是问题根源」——不是。** 两个新录的 OBS 视频是普通 MP4（无 `moof`/`mvex`），无损快速档端到端一样正确：切 `[0, 8.333333]` 与 `[16.666667, 19.4]` 两段拼接得 **11.075 s / 332 帧**（期望 11.067 / 332），每段物理包数 = 真实帧数（250 / 82，无 pre-roll）。真正的根因一直是 `-avoid_negative_ts make_zero`，与容器是否分段无关。**但这些素材的关键帧间隔仍是 8.333 秒**，所以「无损快速只能按关键帧粗切」的限制照旧 —— 决定这一档好不好用的是关键帧间隔，不是容器结构。

**手机 MOV 的两个坑（实测）**：

1. **它不是变帧率**。所有视频包的 `duration_time` 都是 0.0333，`[2.6, 5.6]` 区间正好 90 个包 = 30 fps × 3 秒。相邻 pts 出现 -0.067 / 0.133 这种值是 **HEVC 的 B 帧重排序**（传输顺序 ≠ 显示顺序），不是 VFR —— 判 VFR 不能只看相邻 pts 间隔。
2. **输出侧 seek（无损快速档的写法）在 HEVC + B 帧重排的文件上会静默丢帧**。同样请求 `[2.700125, +2.0]`：

   | 写法 | 结果 |
   | --- | --- |
   | 输出侧 `-i in -ss 2.6991 -t 2 -c copy` | 视频 start=0.934 / 1.233 s / **34 帧 / 37 包**（应 60 帧）✗ |
   | 输入侧 `-ss 2.700125 -i in -t 2 -c copy` | 视频 start=0 / 2.167 s / **62 帧 / 65 包** ✓ |
   | reencode（`libx264 -crf 20`） | 2.009 s / **60 帧** ✓，方向正确 |

   拼接后还会报 `Could not find ref with POC 610` —— 段的参考帧被丢掉了。**同一条输出侧命令在 H.264 素材上完全正常**（2.013 s / 60 帧 / 60 包），所以这不是 seek 写法本身的问题，而是 HEVC 的 dts/pts 重排让 muxer 的「丢弃到目标点」逻辑误判。**结论：只要放开 HEVC，就必须强制走 reencode，绝不能走 fastcopy。**

**旋转不是拒绝的理由（实测）**：copy 档会**保留**旋转元数据（产出仍是 `旋转 -180`，播放器自己会转正）；reencode 档由 ffmpeg 的 autorotate 把旋转**烘进像素**（缩略图对比：源与 libx264 段方向完全一致），只是元数据被清掉 —— 两条路的方向都对。但 copy 出来的段要经过 concat 拼接，**concat 不保证把 display matrix 带进成片**，所以带旋转的素材仍然强制走重编码。

**第三个坑：手机 AAC 的 skip samples 前置边数据会毁掉拼接（0.1.64 修）**。该 MOV 的音频首包带 `Skip Samples,41231`（0.859 秒的前置静音）+ 首包 pts 为负。`-c:a copy` 会把它原样带进每一段，而 concat 按段时间戳累加后整片错位：

| 写法 | 两段各 3 秒拼接后 |
| --- | --- |
| 音频 `-c:a copy` | **6.863 秒**、视频 start=0.858 / 音频 start=0 → 音画差 **0.86 秒**、音频多出 1.87 秒 ✗ |
| 音频改成 `-c:a aac` | **6.021 秒 / 180 帧**、解码音频 6.059 秒、音频 RMS 轮廓与预期**逐块吻合**（≤0.2 dB）✓ |

所以 **reencode 档的音轨也必须重编码**（fastcopy 档保持 copy）。这条推翻了 0.1.63 基于 OBS 素材得出的「`-c:a copy` 两条路都安全」—— 那条结论只对**没有 priming 边数据**的素材成立。另外加了一道兜底：**拼接后用 `media_duration` 校验成片时长**，与各段之和相差超过 0.3 秒就报错并建议改用精确重编码（正常偏差实测只有 0.008 秒），避免静默交付一条错位的片子。

**libx265 的速度代价**：同一条 2560×1920 素材，`libx265 -preset veryfast` 只有 **0.365× 实时**（2 秒画面编码约 5.5 秒），71 秒的素材要三分钟以上。所以 HEVC 输入一律**转 H.264（libx264）**：兼容性更好、速度更快，也只需要探测 libx264 一项能力。

### 切片过渡（0.1.67；转场类型 0.1.72 换）

**需求**：两个片段中间有切除时，接缝处原本是硬切，人物姿态会跳一下（jump cut）。加一段短过渡能把它盖住。四条结论决定了实现形态：

1. **只有「精确编码」档能做过渡** —— 快速档是 `-c:v copy`，画面根本没解码，混合不了。所以档位默认仍是「无损快切」，过渡只在切到精确编码时生效（用户选的是「档位优先」）。
2. ~~**用重叠式溶解**（与剪辑软件的默认「交叉溶解」同义）~~ —— **0.1.72 已换成 `smoothleft` 平滑滑移**，原因见下面「第六个坑」：溶解把两帧不同姿态的画面叠在一起，边缘成双影、细纹交织，肉眼就是「锐化过度 + 密密麻麻的点」。重叠式仍然决定「成片比硬切短 Σd」（画面重叠 d 秒），字幕重映射与「拼接后时长校验」还是要按它校正。
3. **每个接缝单独产一段 d 秒的过渡段**，与普通段一起 concat。这与「对整段跑一次 `xfade(offset = 前段时长 − d)`」等价，但复用同一条链路。**音频不走滤镜混合**（见下面「第二个坑」）：取「两侧被裁掉那一截的前 d/2」硬拼接，合起来正好 d 秒，两端各 5 毫秒微淡。
4. **默认 0.3 秒**：剪辑软件的转场默认是 1 秒（Premiere 的 30 帧、DaVinci 与 Final Cut 的 Standard Duration）、短视频工具默认 0.5 秒，但那些是给镜头切换用的；**消隐跳切**的实操区间是 0.1~0.3 秒。再长就不像在藏剪辑点。可调范围 0.1~1.0 秒、步进 0.1，`0` = 硬切。

**实测（OBS 硬编素材：30 fps / H.264 1080p / 关键帧 1 秒；保留 [0,5] [8,11] [13,15.47]，接缝各 0.3 秒。音频窗口列是 0.1.70 修后的值）**：

| 片段 | 源窗口（视频 / 音频） | 产出 |
| --- | --- | --- |
| 主体 0 | [0, 4.7] | 4.7 秒 / 141 帧 |
| 过渡 0 | 视频 [4.7, 5.0] × [8.0, 8.3]；音频 [4.7, 4.85] + [8.0, 8.15] | 0.3 秒 / 9 帧 |
| 主体 1 | [8.3, 10.7] | 2.4 秒 / 72 帧 |
| 过渡 1 | 视频 [10.7, 11.0] × [13.0, 13.3]；音频 [10.7, 10.85] + [13.0, 13.15] | 0.3 秒 / 9 帧 |
| 主体 2 | [13.3, 15.47] | 2.167 秒 / 65 帧 |
| **成片** | — | **296 帧**（= 141+9+72+9+65 ✓；9.87 秒，容器多出的 ~0.02 秒是音频尾包） |

两条路各跑一遍全通：**未处理音频**（音频窗口取自源）9.888 秒 / 296 帧、音频 9.887 秒；**处理过音频**（音频取自「已拼接时间轴」WAV，4 输入）9.891 秒 / 296 帧、音频 9.891 秒 —— 都解码无报错。

**第一个坑（已修）**：最初把两个视频窗口写成 `bodyEnd − t` 与 `start + t`（各往主体里挪了一个 d），那样**前段的最后一截内容永远不会出现、后段开头会被播两遍**。判据很硬：过渡段首帧应等于**源上 `bodyEnd` 时刻**那一帧（实测 109.768 vs 109.738）、下一主体首帧应等于**源上 `nextStart`** 那一帧（111.584 vs 111.561）。

**第二个坑（0.1.68 修，就是线上「过渡导出失败」的真凶）**：**`acrossfade` 在 0.3 秒级的短窗口上会偶发一帧都不输出**。同一条命令连跑 5 次：4 次得到 5966 / 8310 字节的正常文件，1 次得到 **257 字节**（`.m4a`）/ **92 字节**（`.wav`）的空文件；把视频滤镜也放进来时（4 输入那条）**连续 4/4 全失败**，报 `Could not open encoder before EOF` → `Conversion failed!`。**这与 `-b:a` 码率参数无关**（一开始误判成码率问题，是样本太小的错觉）。改成 `concat` 硬拼接后连跑 6 次**逐字节一致**（950599 字节），音画各自 0.300 秒。**教训**：短窗（亚秒级）上别用 `acrossfade`；判断「是不是偶发」要**同一命令连跑多次看文件大小**，一次成功不能当证据。
　顺带说明：音频不跟着做交叉淡化的另一个理由 —— 口播素材上把相邻两句叠在一起比硬切更难受；硬拼接 + 5 毫秒微淡既没有叠字也没有咔嗒。

**第三个坑（顺带修）**：滤镜参数用**位置写法**（`settb=AVTB`、`setpts=PTS-STARTPTS`）只有 **ffmpeg 7.0+** 认，实测 6.1.1 直接报 `No option name near 'AVTB'`。一律改具名（`settb=tb=AVTB`、`setpts=expr=PTS-STARTPTS`、`fps=fps=<率>`）。另外**有些精简构建连 `xfade` 都没有**（实测某编辑器自带的 6.1.1 报 `Filter not found`），所以 `detect_ffmpeg` 除了 libx264 还要探测 `xfade`，前端据此禁用过渡而不是等导出才炸。

**第四个坑（诊断本身）**：`run_ffmpeg` 原来用 `truncate(&stderr, 300)` 截**开头** 300 字符，而 ffmpeg 的报错在**最后几行**、开头是 `Input #0 … Duration …` 的横幅 —— 结果用户看到的报错只有横幅，真正的原因整段丢掉（这次排查就卡在这）。已改成 `truncate_tail`（保留末尾），并给片段/过渡段失败加上「第 N 个保留区间 / 过渡段（第 N–M 之间）」的前缀。

**第五个坑（0.1.70 修，线上「导出失败：Unable to choose an output format for '4.630'」的真凶）**：`-b:a:192k` 被写成了**一个 token**。ffmpeg 的 `find_option` 只在**第一个 `:`** 处比对选项名，所以 `-b:a:192k` 被解析成「选项 `b`、流标识符 `a:192k`」，并且**把紧随其后的那个 argv 当成选项值吃掉**（值不校验，于是不报「无效码率」）。于是：

| 命令里的写法 | ffmpeg 实际看到 | 结果 |
| --- | --- | --- |
| `-c:a aac -b:a:192k -t 4.630 OUT.mp4` | `-b:a:192k` 吃掉 `-t` | `4.630` 成了**裸输出文件名** → `Unable to choose an output format for '4.630'` |
| `-c:a aac -b:a:192k OUT.mp4`（无 `-t`） | 吃掉 `OUT.mp4` | `At least one output file must be specified` |
| `-c:a aac -b:a:192k -progress pipe:1 … OUT.mp4` | 吃掉 `-progress` | `pipe:1` 成了裸文件名 → `Unable to choose an output format for 'pipe:1'` |

三种报错**都不提码率参数**，所以极难从日志倒推。**实测 ffmpeg 7.1.1（LosslessCut 自带）与 8.0.1（Krita 自带）行为一致**，因此不是版本差异。修法：统一走 `push_aac_bitrate`，永远发两个 argv（`-b:a` + `192k`）。同类写法（`-c:v:0`、`-map:...`）一律回避。

**为什么它能活到 0.1.70（教训比 bug 本身重要）**：前几轮的端到端验证都是**照着手写** ffmpeg 命令跑的，手上下意识写成 `-b:a 192k`（正确形态），而程序生成的是坏形态 —— **手写版能过，真实版必炸**。而且这个 bug 恰好只在「带 libx264 的 ffmpeg + 精确编码档」上暴露：此前自动探测选中的是 Krita 那份**没有 libx264** 的构建，「精确编码」根本走不起来，这段代码等于从未真正执行过。现有的补救：设 `GAP_GONE_FFMPEG_TRACE=1` 跑 `pnpm tauri dev`，`run_ffmpeg` 会把**每条真实命令**原样打到终端（失败时自动打印命令 + stderr）。**验证导出请验程序发出的那条命令，不要照着它重写一遍。**

**0.1.70 端到端复跑（按程序生成的形态，LosslessCut 7.1.1）**：素材 `2026-09-26 17-54-37.mp4`（30 fps / H.264 / 关键帧 1 秒），保留 `[0,5] [8,11] [13,15.47]`、接缝各 0.3 秒 —— 段 141 / 9 / 72 / 9 / 65 帧，成片 **296 帧 / 9.888 秒**，`ffprobe` 解码无报错。与 0.1.68 记录一致。

**注（0.1.70 一并修）**：上表「音频 `[4.85, 5.0] + [8.0, 8.15]`」只对**回写处理后音轨**那条成立，而且它取的是「被裁掉那一截的**后半**」；用**源音轨**时代码取的是 `[body_end − d/2, body_end]`（= `[4.55, 4.7]`）—— 落在主体**已播过**的一截里，接缝处重复约 150 ms 音频，而被裁掉那一截的音频永远不出现。两条分支锚点不一致，且都没跟视频窗口对齐。现在统一成「两侧**被裁掉那一截的前半**」（源音轨 `[body_end, body_end + d/2]` + `[next_start, next_start + d/2]`；处理后音轨换算成 `[audio_after − d, …]` + `[audio_after, …]`），起点与视频窗口的起点重合。

**内容对齐的硬判据（RMS 对照，`atrim=0.005:0.145` 跳过 5 ms 微淡）**：

| 取的位置 | RMS level |
| --- | --- |
| 过渡段音频首 0.15 s | −26.88 dB |
| 源 A @4.705（**新**窗口起点） | −26.84 dB ✓ 差 0.06 dB |
| 源 A @4.555（**旧**窗口起点） | −23.86 dB ✗ 差 3 dB |

两条分支各跑完整链路（段 141 / 9 / 72 / 9 / 65 帧）：处理后音轨 **296 帧 / 9.888 秒**、源音轨 **296 帧 / 9.887 秒**，音频与视频逐段对齐、解码无报错。

**第六个坑（0.1.71 修，用户报「过渡段画面像锐化过度、有密密麻麻的点」）**：过渡段的**第一个视频窗口起点写成了 `body_end − d`**，而应该是 `body_end`。后果是过渡段**倒回去重播**了主体刚播过的最后 0.3 秒，被裁掉的那一截（`[body_end, end]`）反而永不出现 —— 与 0.1.70 修的音频窗口是同一类错（那边是 d/2，这边是整个 d）。**讽刺的是代码注释里窗口写对了、还专门警告「起点不要写成 `body_end - t`」，代码本身照错写；而 `docs.md` 的实测表、AGENTS.md 也一直按正确值记着**，因为这张表来自 0.1.68 的手写验证命令（又见硬约束 19 最后一条）。

**怎么发现的（判据是内容，不是帧数）**：两种窗口的成片**都是 296 帧 / 9.888 秒**、段数与时长相貌完全一样，靠帧数永远看不出来。用 PSNR 对源上已知时刻的帧一比就分开了：

| 过渡段第 0 帧 vs 源 | 修前 | 修后 |
| --- | --- | --- |
| **@4.700**（= `body_end`，正确） | 18.9 dB | **43.97 dB** ✓ |
| @4.400（= `body_end − d`，错误） | **43.79 dB** ✓ | 18.9 dB |

修前后完整链路复跑：段 141 / 9 / 72 / 9 / 65 帧，成片 296 帧 / 9.887 秒，两条音频分支都对齐。

**关于「点点」本身（这不是 bug，是交叉溶解的固有外观）**：取过渡段中间那帧量化 —— 它与 A@4.833 的相似度 23.1 dB、与 B@8.133 22.0 dB（说明确实是 50/50 的真混合）；`edgedetect` 后的平均亮度 **4.39，而同内容普通帧只有 2.64**（约 1.66 倍）；码率 **25.2 Mbps，同内容普通段 5.18 Mbps**（约 5 倍）。也就是说：交叉溶解把两帧不同姿态的画面叠在一起，**边缘成了双影**（看着像锐化过度/发白描边），**细纹理互相交织**（头发、衣物纤维 = 那些「点点」），编码器为此要多花 5 倍码率。这是 dissolve 的定义使然，**不是编码或接线问题**（纯 A 的那一帧 PSNR 43.97 dB，说明转换链路是干净的）。业内用交叉溶解是表达「时间流逝」，而**掩盖跳切**通常用滑移/推入/闪白这类**不叠影**的转场。要改的话是换 `xfade` 的 `transition=`（`slideleft` / `smoothleft` / `wipeleft` 等），不是调参数。

**0.1.72 已换成 `smoothleft`（平滑滑移）**，同一个接缝的对照：

| 指标 | `dissolve`（改前） | `smoothleft`（改后） | 普通帧参照 |
| --- | --- | --- | --- |
| 首帧 vs 源 @4.700 | 43.97 dB | **44.25 dB** | — |
| 末帧 vs 源 @8.267 | 28.20 dB | **36.48 dB** | — |
| 高频能量（`edgedetect` YAVG） | **4.39** | 2.19 ~ 2.60 | 2.64 |
| 码率 | **25.2 Mbps** | 7.0 Mbps | 5.18 Mbps |

高频能量回到普通帧水平 = 双影/点点消失；末帧 36.5 dB 说明滑移在段内就走完了（溶解只到 89%，末帧还挂着前一帧的残影）。完整链路复跑：296 帧 / 9.887 秒、解码 0 错误。

**边界**：过渡时长由前端算好（按帧取整、短段保护 `min(d, 前段÷2, 后段÷2)`），Rust 只兜底钳制；某段太短时那处过渡自动缩短，导出前提示有几处这样处理。预览仍是硬切（过渡只在导出时应用），首尾不做淡入淡出。

### 无损快速档改为「安全吸附」（0.1.66）

**旧规则的问题**：原来要求「切点落在关键帧 ±0.1 秒内」才允许这一档，否则整档降级重编码。1 秒关键帧网格下这个容差的命中率只有约 20%（每次剪切），也就是说用户刚按建议把 OBS 改成 1 秒，绝大多数剪切**仍然**掉回慢且失真的重编码 —— 设置白设。

**新规则**只保留两条安全判据：① 吸附点不得退进上一段保留区间（否则时间轴重叠、把已删内容搬回来）；② 吸附后该区间不能为空。**吸附的语义是「少删」而不是「重排」**：起点只会往前挪到最近的关键帧，`-t`（终点）仍然精确，所以成片最多比「精确删除」多留**一个关键帧间隔**的内容，且多留的必然是删除区间里的那一段（静音），**不会吃掉人声**。代价如实播报（UI：「切点吸附到关键帧，最多少删 X 秒（残留静音，不会吃掉人声）」）。

**连带必须修的**：成片时间轴以吸附后的区间为准，字幕重映射要跟着用 `mapRangeToKept` 的第 4 个参数 `keptOverride`（传吸附后的保留区间），否则 SRT 会随每个吸附点累积错位 —— 旧规则下偏移 ≤0.1 秒可以忽略，新规则下最坏是一个关键帧间隔，不能忽略。

**第四个坑：含 B 帧的素材，无损快速会整段丢掉切点后的第一个 GOP（0.1.65 挡住）**。机制：ffmpeg 的输出侧 `-ss` **按 dts 丢包**，而 B 帧重排让每个关键帧的 dts 比它的 pts 早若干帧（实测该素材早 0.067 秒 = 2 帧）。于是「目标点 = 关键帧 pts」（这正是我们的写法）时，**关键帧自己的 dts < 目标点 → 被丢掉** → 输出文件的开头没有关键帧 → ffmpeg 一直丢到下一个关键帧，正好丢掉**一个完整 GOP**。前三个 OBS 素材（AMD 硬编）`has_b_frames=0`、dts=pts，所以这个坑一直没暴露。实测（关键帧 1 秒的素材，请求 `[0, 5]`）：

| 写法 | 结果 |
| --- | --- |
| 输出侧 seek（当前实现）· 让 1 ms | 122 包 / 5.061 s，**首包 pts=1.000**（丢掉 [0,1) 整个 GOP）✗ |
| 同上 + `-avoid_negative_ts make_zero` / `-fflags +genpts` | 122 包，一样 ✗（加 flag 无效） |
| 同上 · 目标点往前让 100 ms | 149 包，首包 pts=0.100（**开头留 0.1 秒空洞**）✗ |
| 输入侧 seek | 152 包 / 5.067 s（150 + 2 帧参考帧，画面完整）✓ |
| 输入侧 seek + `make_zero`，两段各 5 秒再去 concat | **10.133 s / 334 包**，解码报 `non monotonically increasing dts`（期望 10.0 s / 300 帧）✗ |

**结论**：① 「目标点往前让一点」不是解 —— 让少了丢关键帧，让多了开个空洞，两头不靠；② **输入侧 seek 的段虽完整，但带 pre-roll 的段经 concat demuxer 会时间戳乱序**，所以拼出来的成片是坏的。因此在 probe 里读 `has_b_frames`，**含 B 帧一律禁用「无损快速」**（前端给明确原因），而不是想办法绕。**想用无损快速，就让录像编码器别产生 B 帧** —— 实测的 AMD 硬编输出 `has_b_frames=0`，配合 1 秒关键帧完全可用（150 包 / 5 秒，逐段验证正确）。真要做「B 帧也能无损切」，得改走 MPEG-TS 中转（TS 无 edit list、时间戳显式，LosslessCut 就是这么做的），那是另一件事。

### ffmpeg 选择：系统里常有好几份

同一台机器上实测到三份 ffmpeg，能力差别很大：

| 来源 | 版本 | libx264 | libass（烧字幕） |
| --- | --- | --- | --- |
| Krita 自带（在 PATH 最前，会被朴素探测选中） | 8.0.1 | ✗ | ✗ |
| TRAE 自带 `resources\app\bin` | 6.1.1 | ✓ | ✗ |
| LosslessCut 自带 `resources` | 7.1.1 | ✓ | ✓ |

所以 `detect_ffmpeg` 改成**优先返回带 libx264 的候选**，并把 `hasLibx264` 交给前端；没有它时禁用「精确重编码」并给出指定路径的入口，而不是等导出时报 `Unknown encoder 'libx264'`。

### 指定 ffmpeg 的入口收到设置页 + 失败必须打终端（0.1.69）

**入口统一**：原先有两处能指定 ffmpeg —— ffmpeg 引导弹框里的「手动指定 ffmpeg 路径」按钮，以及设置页内一处提示旁的「指定 ffmpeg」；两处并存时「当前生效的是哪一份」说不清。现在只留**设置页的「ffmpeg 路径」**（一行文本输入 + 右侧「选择…」按钮，走 `dialog.open` 挑 `exe`），即时生效：失焦或选完立刻重探一次并回报结果，下一行显示探测到的版本与 `libx264` / `xfade` 能力。引导弹框只剩安装引导 + 重新检测 + 指回设置页的提示。Windows 上未设置时的默认值是 `D:\ProgramData\LosslessCut-win-x64\resources\ffmpeg.exe`（就是上表里那份**带 libx264 也带 libass** 的 7.1.1）；**指定路径失效时会回退到自动探测**，所以这份默认值换台机器也不会把视频功能锁死。用户填的路径也进设置页快照，「取消」会一起回退。

**为什么顺手改这个**：上表那份 LosslessCut 的 ffmpeg 恰恰是唯一同时满足「精确编码 + 烧字幕」的，而它在 PATH 里排最后 —— 默认值直接指向它，能省掉每次重装/换机后手动指定。

**失败必须打终端**：这次报障暴露出两个「看不见」——① 界面上只显示了 `truncate(&stderr, 300)` 截下的**开头**，即 `Input #0 … Duration …` 的输入横幅，真正的 `Conversion failed!` 在末尾被切掉（已改 `truncate_tail`）；② **终端上一个字都没有**，因为 Rust 侧的错误全都只 `return` 给前端。现在 `video.rs` 统一走 `log_line` / `log_info` / `log_error` / `fail`（前缀 `[gap-gone][阶段][级别]`），ffmpeg/ffprobe 失败连**命令行 + stderr 末尾 2000 字符**一起打（`format_command` 还原成可复制粘贴的一行），并给导出计划落一行 info（档位、保留区间、逐接缝过渡、是否回写处理后音轨、输出路径）。写进 AGENTS.md 硬约束 20。

### 边界（拒绝导入）

无音轨、多音轨、变帧率、音画起点相差 > 50 ms。这些都是「检测到即提示不支持」，不做半吊子兼容。

**HEVC 与带旋转元数据的素材已于 0.1.64 放开**（实测后决定）：允许导入，但强制走精确重编码（HEVC 转 H.264），并把「无损快速」的适用条件收紧为「整段保留，或 H.264 + 无旋转 + 切点对齐 + 音频未处理」。

## 平台权限

* macOS 使用 `Info.plist` 的 `NSMicrophoneUsageDescription` 和音频输入 entitlement。

* Windows 处理桌面应用麦克风权限、设备占用和蓝牙设备切换。

* Linux 保持可构建，后续验证 ALSA、PulseAudio 和 PipeWire。

