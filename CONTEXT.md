# Gap Gone 领域词汇

## AudioAsset

可被播放、编辑或导出的音频内容。原始录音和降噪后的版本都是独立的 AudioAsset，原始版本始终保留。

## RecordingTake

一次录音的生命周期。它从 3 秒准备倒计时开始，倒计时期间不采集音频；随后进入录音中和录音预览，最后被确认并转化为可编辑的 AudioAsset，或被取消并丢弃。

## TimeRange

音频时间轴上的半开区间 `[start, end)`。`start` 不得小于零，`end` 不得大于音频时长，且 `start < end`。

## EditTimeline

当前 AudioAsset 上需要跳过的 TimeRange 集合。区间按时间排序、互不重叠；它描述编辑结果，不改变原始 AudioAsset。内部区分手动切除区间与自动静音检测区间，最终播放和导出前再合并。

## SilenceDetectionCandidate

由静音分析生成、尚未应用到 EditTimeline 的候选 TimeRange 集合。候选区间只显示预览，不影响播放或导出；用户点击应用后才成为自动检测区间。

## PlaybackSession

一次连续的播放过程。它负责把编辑时间轴映射到原始音频时间，并在播放过程中跳过 EditTimeline 中的区间。它同时持有两个**会话内**的试听参数：播放速度（0.6～2.0 倍速，变速不变调）与循环开关（在成片范围内回环，回环点为成片起点）。两者只改变听到的样子，不改变 AudioAsset、EditTimeline 或导出的内容。

## MonitorLevel

录音输入的实时反馈，包括 RMS、Peak、峰值保持、数字削波状态，以及从开录起累计的 Integrated LUFS。电平条使用 dBFS 表示，-6 dBFS 以上属于预警，-12 ~ -6 dBFS 是期望区间并在表上画成目标带，数字削波提示可单独清除。

两侧电平表必须同口径：录音侧由原生端每 100 ms 聚合上报，播放侧也从当前播放位置取 100 ms 窗口计算；稳定读数一律用整段样本峰值（录音侧的「保持」与播放侧的「整段」），只有条子和峰值针走瞬时值。否则同一段音频会因为测量窗口不同、以及一侧有保持另一侧没有，差出语音波峰因数那十几 dB，用户根本无法判断。

表形上，录音侧是「RMS 底柱 + 瞬时峰值白针 + 保持白针」三条：底柱代表说话的平均水平，两根针分别代表当下尖峰与历史最高。播放侧竖条同理是「RMS 填充柱 + 瞬时峰值白针」。两处的柱与针都不加 CSS 过渡，靠逐帧刷新驱动 —— 过渡叠在窗口之上会拖尾，看起来像表在迟缓。

目标带（-12 ~ -6 dBFS）只管峰值，是用来看白针落点的；RMS 底柱本来就低于峰值 8 ~ 12 dB，不该拿它去对带子。

LUFS 对照用户设定的目标响度（设置里可选，默认 -19 LUFS，另有广播 -23 与短视频 -14），偏差超过 1.5 dB 才提示偏弱/偏响，只提示不拦截。MonitorLevel 只描述测量结果，不代表是否把输入声音回放到输出设备。

## NoiseReductionVersion

基于某个 AudioAsset 生成的独立降噪版本，包含模型版本、强度预设和处理区间。降噪失败时，原始 AudioAsset 不受影响。

## VoiceChain（语音优化链）

界面上仍是「压缩」按钮，但它从 0.1.49 起是一条链路：**高通 80 Hz → 向下扩展 → 软拐点压缩 → 响度标准化**。前三步只把动态收窄、把音色修顺，不负责整体响度；**响度与真峰值上限由末尾的归一唯一定**，真峰值限幅在整条链里只出现一次。

**档位**（轻 / 中 / 强）不预设阈值，只声明**目标跨度**：轻 8 / 中 5 / 强 3 dB，回答「想把这段录音的动态压到多窄」这一个问题。

阈值与压缩比由素材自身反推（`analyseSource` + `planCompression`）：逐 100 ms 算块电平，用**锚在噪声底上的门限**（`噪声底 + 14 dB`）剔掉气口与停顿，再取 P10 / P90，**输入跨度 = P90 − P10**；压缩比 ≈ 输入跨度 ÷ 目标跨度（封顶 4:1），阈值取在 P10 附近（6 dB 软拐点），让最低的有效段基本不动、只压大的。跨度是相对量 —— 麦克风增益整体平移时 P10 / P90 同步平移、差值不变，所以增益拧大拧小、甚至换一支灵敏度完全不同的麦克风，都不改变判定结果。统计只算**保留区间**（与响度测量同口径），切掉一段响噪音不会再把压缩比撑大。

门限**锚在噪声底上、不锚在中位数上**（0.1.60 修）。中位数会随压缩移动，而压缩就是这条链的一环：压缩把语音压低十几 dB → 中位数与门限一起下降 → 原本被剔掉的气口重新进入统计，「输出跨度」实测反而比输入更大。噪声底是气口的电平，压缩在气口处增益≈0，它不随压缩移动；量压缩输出时要把**输入的**噪声底当锚传进去，前后才是同一批块，播报里的「跨度 A → B」才可比。门限另外夹在「中位数」以下 —— 素材全程连续说话、根本没有气口时，块电平的 P10 不是噪声底而是最轻的语音，门限高过中位数就会把一半动态当成噪声切掉。

**向下扩展**（不是硬门）：阈值夹在「噪声底」与「语音 P10」之间（`min(噪声底 + 6 dB, P10 − 3 dB)`），阈值之下按 2:1 衰减、最多 12 dB，用来在停顿与气口处按住底噪 —— 否则归一把整段抬到目标响度时，底噪会一并被抬起来。两条自动跳过规则：底噪比语音有效电平低 30 dB 以上（信噪比已经够好）时不做；噪声底与语音挨得太近、夹不出余量时也不做。扩展器做错比不做更糟，宁可跳过。

**曾经试过、已移除的「去嘶声」**：0.1.56 曾加过一段只动 7 kHz 以上的高频压制，用来收拾「人声底下那层嘶嘶」。0.1.58 移除 —— 支撑它的听感证据其实来自当时的降噪兜底算法，而不是 DeepFilterNet（见 AGENTS.md 硬约束 18）；引擎正常工作后再叠一层最多 10 dB 的高频衰减只会把语音削闷。要加回来必须先有「DFN 正常工作时」测到的残余高频噪声证据。

**压缩器不做自动补偿**（0.1.46 的「按最深处补偿」已在 0.1.49 删除）：补偿是静态抬全段、抬完由限幅器收拾，波形会变成平顶；而且末尾的归一按实测 LUFS 重算增益，会把这份补偿在数学上抵消 —— 收益归零，限幅留下的增益包络却不可逆。「峰值没变小」由「目标 LUFS + 限幅上限」共同保证，播报直接给最终真峰值与限幅衰减。

三种边界都如实播报，不假装：压缩比顶到 4:1 仍达不到目标跨度 → 标记 `capped`；素材跨度与目标差不到 1 dB → 标记 `skipped`，压缩这一步原样返回输入缓冲，但链路仍继续走响度标准化；剔除气口后有效块不足 2 秒（短录音）→ 走兜底参数「成片有效电平 − 3 dB、压缩比 2:1」。

检测器用**真 RMS 的一阶指数平滑**（压缩器 30 ms、扩展器 10 ms），不是峰值包络。语音的峰值比有效值高 8 ~ 12 dB，按峰值判触发会让阈值照着尖峰设，结果只有偶发尖峰被压、整段起伏纹丝不动（0.1.43 的三档 -14 / -18 / -22 就是这个病）。播报里的「成片有效电平」是块电平中位数（已剔除 -60 dBFS 以下静音块），用户可据此校准录音增益；「噪声底」是块电平 P10。

撤销粒度是两级：「撤销压缩」整体回退到链路前（含链路里那次归一）；「撤销响度」只退归一那一步、保留链路结果（`loudnessBaseRef` 存的就是链路输出、归一前那一版）。两个按钮同时亮时，先撤响度再撤压缩，顺序自然衔接。

基准语义是这个版本的核心：链路永远从「最近一次的输入」重算，所以重复点「压缩」等于换档位，不会叠加；基准在新录音、导入、恢复原始、确认降噪时清空。响度标准化不影响链路基准，但反过来不成立 —— 归一是按旧动态算出的静态增益，链路一旦改动动态，那次归一即失效。

## VideoAsset（视频素材）

可被播放、编辑、导出的视频文件，内含视频轨与音频轨。导入时不整段进内存，也不复制文件：只把原文件路径传给 ffmpeg 抽出音轨，喂给现有 AudioAsset 管线（波形 / 静音检测 / 切除 / 播放全部复用），视频轨与探测元信息（时长、编码、音轨有无、关键帧位置）仅以文件路径 + 探测结果的形式保留。导出时按 EditTimeline 同步切除视频与音频两轨；应用过降噪 / 响度归一化的音轨会回写进导出视频。

音轨抽出保持**源文件的原始采样率与声道数**，不在这里下混或重采样：编辑期听到的就是原声，采样率 / 声道转换只在需要时（降噪）由处理链自己做。波形与电平表按声道下混成一条显示，与处理链的口径保持一致；立体声源在未处理音频时导出仍是原音轨。

## VideoExportVariant

视频导出的编码策略，默认走「无损快速」那一档。

- `fastcopy`（默认）：画面不重编码（`-c:v copy`），音频未处理过时也原样拷贝，几乎零耗时、画质无损。**只能从关键帧起切**，所以它的可用条件是「整段保留（只是重新封装），或者 H.264 + 无旋转 + 无 B 帧 + **切点能安全吸附到关键帧**且音频未被处理」；不满足时导出自动降级为 `reencode` 并说明原因。**吸附的语义是「少删」而不是「重排」**：起点只会往前挪到最近的关键帧，因此成片最多比「精确删除」多留一个关键帧间隔的内容（残留的是静音，不会吃掉人声）；只有吸附点会挪进上一段保留区间（时间轴重叠）才算不安全。代价（本次最多少删多少秒）由 UI 如实告知。**注意成片时间轴以吸附后的区间为准**，字幕重映射必须跟着用吸附后的区间，否则会随每个吸附点累积错位。
- `reencode`：逐保留区间切片后拼接，重编码 H.264 + AAC（音轨也要重编码，源音轨的 priming 边数据会让拼接错位），切点精确到帧。慢且有损，需要所选 ffmpeg 带 libx264。
- **HEVC 与带旋转元数据的素材只能走 `reencode`**（0.1.64 起允许导入）：HEVC 上不重编码的切片会静默丢画面，带旋转的素材拼接后方向不保证正确；HEVC 输出统一转 H.264。
- **含 B 帧的素材也只能走 `reencode`**（0.1.65）：不重编码的切片在 B 帧重排的流上会整段丢掉第一个 GOP（ffmpeg 的输出侧 `-ss` 按 dts 丢包，关键帧的 dts 早于其 pts）。x264 软编常见，AMD / NVIDIA 硬编一般没有 —— 想用 `fastcopy` 就选不产生 B 帧的录像编码器。
- **切片过渡（`reencode` 独有，0.1.67 起；音频语义 0.1.68 改；转场类型 0.1.72 改）**：设置里的「切片过渡时长」按**逐接缝**生效，`0` = 硬切。每个接缝从相邻两段的边界各取 d 秒做过渡：**画面是平滑滑移**（`xfade` 的 `smoothleft`，两段横向推入衔接），**声音不是交叉淡化** —— 音频取「本段被裁掉那一截的前 d/2 + 下段被裁掉那一截的前 d/2」硬拼接并加 5 毫秒微淡（`acrossfade` 在 0.3 秒级短窗口上偶发空输出，且口播素材上交叉淡化会把相邻两句叠在一起）。**不用交叉溶解**：它把两帧不同姿态的画面叠在一起，边缘变双影、细纹理互相交织（用户看到的就是「像锐化过度 + 密密麻麻的点」，实测高频能量是普通帧的 1.66 倍、码率 5 倍）；交叉溶解适合表达时间流逝，而这里的用途是掩盖跳切。因此：
  - **成片比硬切短 Σd**（不是变长），字幕重映射与「拼接后时长校验」都必须按 Σd 校正；
  - 视频窗口只能用**被裁掉的那两截**（本段 `[bodyEnd, bodyEnd + d]` 与下一段 `[start, start + d]`），用错了会把主体内容丢一截或重复播一遍（`bodyEnd − d` 这个错法活到 0.1.71，靠帧数看不出来 —— 帧数、时长、码流结构全正常，只有内容不对）；两侧音频各 d/2 秒，取自那两截的**前半**（起点与视频窗口重合），合起来正好 d 秒以保持音画同步；
  - **短段保护**：每处接缝最多取相邻两段各一半，否则会把短段吃光；
  - 时长按**帧**取整（`round(d × fps)`），避免 ±1 帧 × 接缝数的偏差把字幕推歪；
  - `fastcopy` 永远硬切：画面是 `-c:v copy`，没有解码后的画面可供混合；所选 ffmpeg 不带 `xfade` 时也退回硬切（导出前提示）。
- **处理过音频（降噪 / 压缩 / 响度归一化）只能走 `reencode`**：回写的成片音轨是「保留区间拼接而成」的时间轴，起点没法跟着视频吸附到关键帧，两条流会各自 seek 而错位。
- 烧录字幕必须重编码画面，因此它只在 `reencode` 下成立。

## 领域边界

- 取消 RecordingTake 不会进入编辑页，也不会留下临时音频。
- 打开或确认新的 AudioAsset 会清空上一份 AudioAsset 的 EditTimeline。
- 静音检测必须先生成 SilenceDetectionCandidate，应用前不能改变播放或导出结果。
- 恢复最近一次自动检测只移除自动检测区间，不能覆盖手动切除区间。
- “去静音/跳过”会缩短播放和导出后的时长；它不等同于保留时长的静音效果。
- 播放速度与循环属于 PlaybackSession：不持久化、不写进 AudioAsset，导出永远是 1.0 倍速的原内容。
- 循环播放的范围是成片（跳过 EditTimeline 后的内容），回环点是成片起点而不是文件 0；成片为空时不循环、不空转。
- 设备权限拒绝、设备拔出和设备占用都是可恢复的录音错误。
- 全部时间轴被跳过时，产品必须显示明确的空结果，而不是创建一个无效的零长度音频。
- 降噪是可撤销的派生操作，不覆盖原始录音。
- 压缩是可撤销的派生操作，不覆盖原始录音；重复点「压缩」是替换（换档位）而不是叠加。
- 「响度标准化」按钮仍是单纯归一、行为不变；但点「压缩」会连归一一起做完（归一是链路末尾的固有环节），因为分两步点容易漏。链路改动动态后，之前单独做过的那次响度归一即失效，会被自动作废并重算 —— 不提示用户重新点一次。
- 语音优化链只做整段的处理，不做段落级的自动电平补偿（自动增益 / AGC）。「离麦克风半米远也能保持音量一致」那是另一条处理链（自动电平：逐句测响度 → 逐句给增益 → 限速平滑 → 静音段冻结增益），目前未实现。
- 视频音频编辑（降噪、响度归一化）结果回写进导出视频音轨，一步到位；未应用任何音频效果时，导出直接用 ffmpeg 切原音轨（保声道数 / 质量）。转录只读音轨、不回写。
- 视频导入/导出依赖系统 ffmpeg（不随应用打包）：检测不到 ffmpeg 时弹框引导安装，视频功能不可用。
- **指定的 ffmpeg 是「优先使用」而不是「必须使用」**：用户在设置里填了路径就先按它探测，跑不起来（文件不存在 / 不是可执行文件）会自动回退到系统 PATH 探测 —— 换机器或升级移动了那份 ffmpeg 之后，用户仍能打开视频，不会因为一条陈旧配置把功能锁死。反之，探测到的这份能不能支撑某个档位由能力决定（`hasLibx264` / `hasXfade`），档位不可用时如实禁用并说明缺什么，不静默换算法。设置页的「ffmpeg 路径」是**唯一**的指定入口。
- 视频导出输出固定 MP4（H.264 + AAC），不跟随输入容器。
